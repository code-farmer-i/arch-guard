# 架构审查：两份「完整配置」样板

> 审查对象：`examples/full/`（`canonical()` 三根拓扑）与 `examples/full-fsd/`（`fsd()` 六层切片）。
> 方法：① 结构度量（角色分布 / 层间边 / 扇入）② 演进成本实测（改一处要动几个文件）
> ③ 盲区实测（拿真实世界会出的问题撞门禁，看哪些**没人管**）④ 两份逐文件审查（按维度出 finding）。
> **只读审查**，不改示例；结论要落地的部分才写成需求（`REQUIREMENTS.md`）。

---

## 1. 结构度量

| 度量               | canonical（`full`）                         | FSD（`full-fsd`）                                                               |
| ------------------ | ------------------------------------------- | ------------------------------------------------------------------------------- |
| 源文件             | 61                                          | 60                                                                              |
| 角色种数           | 21                                          | 30                                                                              |
| index / 公开面文件 | 5（导出名共 9）                             | **19**（导出名共 35）                                                           |
| 最高的扇入文件     | `shared/config/paths.ts`(6)                 | `shared/config/index.ts`(4)                                                     |
| 层间边（主要）     | `app→modules` `modules→shared` `app→shared` | `app→pages/widgets` `pages→features/entities/shared` `features/entities→shared` |
| 反方向边           | 0                                           | 0                                                                               |

**读法**：两套都是"单向、无反向依赖"的健康形状 ✓。差异来自范式：FSD 的公开面是**规范要求**（每个切片一个 `index.ts`），
所以 index 文件多 4 倍、导出名多 4 倍 —— 这是**规范的成本**，不是我们的设计问题；代价是"每个切片都要维护一个公开面"。

---

## 2. 演进成本实测（同一类改动要动几个文件）

| 改动                                 | canonical               | FSD          | 说明                                                                        |
| ------------------------------------ | ----------------------- | ------------ | --------------------------------------------------------------------------- |
| 换缓存键前缀                         | **1 个文件**            | **1 个文件** | `shared/api/queryKeys.ts` ✓ 唯一出处生效                                    |
| 换 UI 组件库                         | **0 个文件直接引 antd** | 同           | 组件一律经 `shared/components/ui`（canonical）/ `shared/ui/<child>`（FSD）✓ |
| 换环境变量名                         | **1 个文件**            | 同           | `shared/config/env.ts`（S44 落点）✓                                         |
| 换埋点事件名                         | **1 个文件**            | 同           | `shared/lib/analytics/events.ts`（D24 落点）✓                               |
| **域 / 切片改名**（`crews`→`teams`） | **12 个文件**           | **12 个**    | 见下                                                                        |

**改名 12 个文件的成分**（canonical）：

```
目录/导入路径（机械改动）        3 个   app/router · modules/crews/routes · modules/crews/views
生成物（重新生成，不该手改）      1 个   shared/api/generated/crews.gen.ts
路由路径（语义，要改）            1 个   shared/config/paths.ts
缓存键（语义，要改）              1 个   shared/api/queryKeys.ts
i18n 命名空间（语义，要改）       2 个   locales/{zh-CN,en}/index.ts
埋点事件名（**外部数据契约**）     2 个   lib/analytics/{events,index}.ts
后端端点（**外部契约**）          1 个   shared/api/client.ts
测试断言引用                      1 个   shared/lib/storage.test.ts
```

**结论**：12 不是"耦合"，而是"同一个词跨了 5 层语义"（目录名 / 前端路由 / 后端端点 / 缓存键 / 埋点事件名）。
好消息是**每一层都各自只有一处**（外部契约的那两个都在单文件里）—— 也就是说，改名时**每一处都要人做一次判断**，
而不是"改一处漏三处"。这是"唯一出处"架构该有的样子。**但**：`/crews` 同时是**前端路由**（`paths.ts`）与
**后端端点**（`client.ts`）的值 —— 值相同、语义不同，读者容易以为它们是一件事（改路由会以为改了接口）。

---

## 3. 盲区实测：这三类问题今天**没人管**

拿真实世界会出的问题去撞门禁（`full` 副本，改完跑 `node es/cli.js`）：

| 撞的问题                                                                     | 结果                               | 判定                                                                                 |
| ---------------------------------------------------------------------------- | ---------------------------------- | ------------------------------------------------------------------------------------ |
| **后端端点第二个出处**（另一个文件里再拼一次 `` `${API_BASE_URL}/crews` ``） | **不报**（只报了撞进去的 S13/S18） | ✗ **真空白**：端点没有"唯一出处"受体，改接口时漏一处 = 404                           |
| **缓存键形状漂移**（`detail: ['crews','detail',id]` 而 `list: ['crews']`）   | **不报**                           | ✗ **真空白**：`invalidateQueries(['crews'])` 前缀不匹配 → **缓存不失效、数据不刷新** |
| **同一份状态两处持有**（页面里再 `useState(selected)` 拷一份 store 的值）    | **不报**                           | ✗ 空白，但判据偏形态 —— 需要真实场景才好定                                           |

另外核了规则家族：**98 条 = 结构 42 · 设计 25 · 文案 7 · 依赖 10 · 度量 8 · 反退化 6**，
**没有任何一条**碰 endpoint；也**没有**碰 loading / error 态。

---

## 4. 两份示例的「非范式强制」差异（需要明文对照，否则"抄哪个"取决于你打开哪个）

| 维度         | canonical                                  | FSD                                                     | 是范式强制吗                             |
| ------------ | ------------------------------------------ | ------------------------------------------------------- | ---------------------------------------- |
| 取数落点     | `modules/<域>/hooks/use*.ts`（**域持有**） | `pages/<切片>/api/use*.ts`（**页面持有**）              | **不是** —— 两种都合法，但复用时成本不同 |
| 页面导出形态 | 默认导出（S13 要求）                       | 命名导出 + index 公开面                                 | 是（槽位语义 vs 公开面语义）             |
| 客户端状态   | `shared/stores/shell.ts`（S41 专用槽位）   | `shared/lib/shell/{index,store}.ts`                     | 是（FSD 的 shared 没有 stores 段）       |
| 共享组件形态 | `shared/components/ui/AppButton.tsx` + css | `shared/ui/app-button/{index,Comp.tsx,Comp.module.css}` | 是                                       |
| 样式令牌     | `shared/styles/{tokens,vendor}`            | `app/styles/{tokens,vendor}`                            | 是（官方 app 有 styles 段）              |
| 公开面数量   | 5 个 index                                 | 19 个 index                                             | 是（FSD 规范要求切片公开面）             |

**结论**：唯一"范式没规定、但我们两套示例给了不同答案"的是**取数落点**（域/实体持有 vs 页面持有）。
页面持有在"该数据被多个页面用"时必然要重构；域/实体持有从一开始就能复用。这条要么统一，要么写进 `PARADIGM.md` 说清取舍。

---

## 5. 逐文件审查 finding

### 5.1 Lead 自查（度量 + 读码得到，证据可复现）

| #   | 位置                                                                                             | 问题                                                                                                            | 为什么是问题（场景）                                                                                                                                 | 严重度 | 建议                                                                                            |
| --- | ------------------------------------------------------------------------------------------------ | --------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ------ | ----------------------------------------------------------------------------------------------- |
| L1  | `full-fsd/src/pages/crews/api/useCrews.ts:6`                                                     | 取数 hook 返回 `string[]`（`data.map(i => i.name)`），canonical 的同类 hook 返回 `CrewRow[]`                    | 两套示例对"hook 给什么"给了不同答案；且页面拿不到实体（要 id/状态就得改 hook）→ 10x 时反复重构                                                       | 中     | 统一返回**实体**（领域模型），"要名字"在页面里做                                                |
| L2  | `shared/api/types.ts`（两边） vs `modules/crews/model/types.ts` / `entities/crew/model/types.ts` | `CrewDto{id,name}` 与领域模型**结构完全相同**、**没有映射层**                                                   | 示例里看不出问题，照抄到真实项目：DTO 有 15 个字段、领域模型只要 5 个时，要么领域被后端契约绑死，要么直接拿 DTO 当模型（S21 拦不住 —— 这是建模问题） | 中     | 示例里加一层显式映射（`toCrew(dto): Crew`），并在 PARADIGM.md 写明约定                          |
| L3  | `shared/config/constants.ts`（两边）                                                             | 按**本质**命名的文件（官方点名的坏味道：`utils` / `helpers` / `constants`）                                     | 今天只放 2 个分页常量；10x 会变成"什么都往里塞"的 dump，且没人能判断什么该进去                                                                       | 中     | 按用途命名（如 `paging.ts`），或把策略数字并进 `queryClient.ts`（D20 的家）                     |
| L4  | `shared/api/client.ts`（两边）                                                                   | 端点内联在 4 个函数里、且都是模板串（`` `/crews?limit=${n}` ``）                                                | 端点没有名字也没有唯一出处：第二个文件里再拼一次**今天没有任何规则会报**（§3 实测）；改名漏一处 = 404                                                | 高     | 加端点表（`endpoints.ts`）+ 新受体；**前置**：事实模型要能看见模板串（§7 可行性）               |
| L5  | `shared/api/queryKeys.ts`（两边）                                                                | 多个模块/切片的键集中在一个文件                                                                                 | 键本是域的知识；域多了这个文件变成"所有域的键大杂烩"，且 `invalidate` 的前缀规约**没有判据**（§3 实测漏报）                                          | 中     | 二选一：① 保持集中但在文件头写明"这是集中式键注册表" ② 让 `queryKeyFrom` 收多落点，键跟着模块走 |
| L6  | 两套示例的 `shared/config/index.ts` 等 barrel                                                    | FSD 侧所有共享配置都经 `shared/config/index.ts` 中转                                                            | FSD 允许（段公开面），但 barrel 会削弱 tree-shaking，且让"谁依赖什么"变模糊（扇入 top1 就是它）                                                      | 低     | 示例里可以只在必要时给公开面（组件/库需要），纯常量文件直接引文件                               |
| L7  | 两边都只有 1 个测试（`shared/lib/storage.test.ts`）                                              | 域逻辑（`modules/crews/lib/format.ts` 等）没有测试；`metrics({ requireTestsFor })` 只点名了 `src/shared/lib/**` | 示例是"照抄对象"：抄的人会以为"测试只要覆盖 shared/lib 就行"                                                                                         | 低     | 把 `requireTestsFor` 扩到域/切片层，示例补 1 个域逻辑测试                                       |

### 5.2 逐文件并行审查（canonical / FSD 各一份，10 个维度）—— 合并去重

两份**独立**审查得出了同一批形状（这点比单份结论可信）：**凡是随域/切片增长的东西都被放进了 shared**。

| 编号 | 形状                                                                                                                                                                                                          | 两份都发现？ | 严重度                  | 证据（谁疼）                                                                                                   |
| ---- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------ | ----------------------- | -------------------------------------------------------------------------------------------------------------- |
| C1   | **数据契约集中在 shared**：端点 / DTO / 缓存键 / 请求策略 / 分页常量全在 `shared/api` + `shared/config`，域/实体层只剩"读取壳"                                                                                | ✅ 两边      | **高**                  | 10x（~50 域）时这 5 个文件是 5 条横向 merge 队列；下线一个域要跨 6 处删；`queryKeyFrom` 单值把集中锁死         |
| C2   | **跨域/跨切片没有合法协作通道**：canonical 的域公开面只有 `routes.tsx`（`isolate` 禁直连 + S23 禁绕过），FSD 的 `@x` 未实现                                                                                   | ✅ 两边      | **高**                  | 域之间要共享实体时的唯一出路是"抬进 shared"—— 而 shared 正是 C1 的病灶；抬得越多域边界越像装饰                 |
| C3   | **埋点在 render 体内调用** `sendEvent(...)`（每个页面各抄一遍）                                                                                                                                               | ✅ 两边      | **高**（canonical）/ 中 | 每次重渲染都上报（含 StrictMode 双调用）→ 埋点虚高、计费失真；D24 只看事件名是不是字面量，看不见调用位置       |
| C4   | **hook 把加载/错误态从接口上抹掉**（`useCrews(): string[]` + `data ?? []`）                                                                                                                                   | ✅ 两边      | 中                      | 后端 500 与"没数据"长得一样 → 故障静默；10x 时每个 hook 都抄一遍                                               |
| C5   | **DTO 与领域模型双份、无映射层**（`CrewDto` vs `CrewRow`/`Crew` 结构相同，恰好兼容）                                                                                                                          | ✅ 两边      | 中                      | 后端加字段：DTO 变、模型不变 → TS 不报错、字段静默丢失；实体组件收裸值（`CrewCard` 收 `name`），实体层名存实亡 |
| C6   | **四个全局 CSS 没有入口**（palette/theme/base/vendor 无 import、index.html 无 link）                                                                                                                          | canonical    | 中                      | **门禁绿但跑起来是坏的**：照抄后主题静默不生效                                                                 |
| C7   | **语义令牌当反色用**（`color: var(--surface)` + `background: var(--brand)`），而 `contrastPairs` 只声明了必然通过的那一对                                                                                     | ✅ 两边      | 中                      | 浅色下白字橙底 ≈ **3.1:1**（低于配置自己声明的 4.5:1）；dark 反成 5:1 —— 达标与否随主题反转                    |
| C8   | **i18n：复数缺失 + 命名空间归属**（传 `{count}` 但无 `_one/_other` → en 恒 "1 orders"；命名空间按域放 shared，加切片要手改两个 `index.ts`）                                                                   | ✅ 两边      | 中                      | 英文界面第一屏就露；10x（50 域 × 多语言）全部文案进主包                                                        |
| C9   | **状态被锁死在一处**（canonical `shared/stores/**` + S41；FSD `shared/lib/shell` 且写者无人调用），域内状态无处放                                                                                             | ✅ 两边      | 中                      | 两域争用同字段谁后写谁生效；10x 时每页临时 UI 状态都往这里塞                                                   |
| C10  | **缓存键带参、请求不带参**（`customerKeys.list(keywords)` 但 `fetchCustomers(CUSTOMER_PAGE_SIZE)`）                                                                                                           | canonical    | 中                      | 不同关键词各建一份缓存却拿同一份数据 → 搜索/筛选必然错（D22 只查字面量在不在家，报不出来）                     |
| C11  | **参数化路由没有表达方式**：`PATHS` 只有静态字面量，`/crews/:id` 无处可放                                                                                                                                     | canonical    | 中                      | 域内写 `` `/crews/${id}` `` → **模板串不是字符串字面量，D23 看不见** → 唯一出处出现"暗门"                      |
| C12  | **测试是常量自证**（唯一测试断言"常量等于字面量"），`requireTestsFor` 只盖 `shared/lib/**`                                                                                                                    | ✅ 两边      | 低                      | 示范出的测试形态就是自证；域内最好测的纯函数零要求                                                             |
| C13  | **注释 / 保留名与实际不符**：`reservedNames` 里有 `common` 而 `shared/components/common/` 真实存在；`index.html` 注释指向不存在的 `config/storage.ts`；同一事件两个名字；FSD 配置注释把 `tokenDir` 写成旧路径 | ✅ 两边      | 低                      | 这份文件是"照抄模板"，注释是读者理解落点的主要入口                                                             |
| C14  | **`crew-filter` 是展示复合块（列表 + 控件），不是"用户动作"**，真实筛选态在上层                                                                                                                               | FSD          | 中低                    | 照抄下去 `features/` 变成第二个 `widgets/`，层级判据失效而页面变空                                             |
| C15  | **`PATHS.login` 没有对应路由/页面**（AuthGuard 却往它跳）                                                                                                                                                     | FSD          | 中低                    | 照抄后未登录用户被跳到不存在的路由                                                                             |
| C16  | **`@x` 未实现**（官方实体互引唯一出口）                                                                                                                                                                       | FSD          | 中低                    | 实体交叉引用时只剩"复制 / 上提 / 违规"三条路                                                                   |

**两份审查共同"做得对"的**：依赖方向 0 反向边 · 引用一律走公开面 · 令牌三层（palette→theme→组件）·
六类唯一出处各一处且都被规则锚住 · app/shared 段间自由互引且没用已弃用的 `processes` ·
切片/域命名（实体单数、页面复数）· 装配可扩展（新增域=一行 import + 一行展开）。

---

## 5.5 覆盖图：架构关切 ⇄ 今天的规则

这张表是"从架构倒推开发"的桥：左列是审查里真正会疼的关切，右列是**今天有没有规则管**。

| 架构关切                                                 | 今天                     | 规则                  |
| -------------------------------------------------------- | ------------------------ | --------------------- |
| 层序单向 / 无反向依赖                                    | ✓                        | S21                   |
| 同层组（域 / 切片）隔离                                  | ✓                        | S22                   |
| 跨组只许走公开面                                         | ✓                        | S03 S05 S23           |
| 孤儿 / 可达性                                            | ✓                        | S15                   |
| 命名契约 / 导出形态 / 重名 / 导出宽度                    | ✓                        | S12 S13 S17 S19       |
| 目录契约（槽位 / 段）与深度                              | ✓                        | S01 S02 S25           |
| 规模（组数 / 子项 / 体积 / 扇入扇出 / 组耦合）           | ✓                        | S16 S26 S27 S34 S39   |
| 单一出处：缓存键 · 路由路径 · 埋点事件名                 | ✓                        | D22 D23 D24           |
| 单一出处：设计令牌 / 颜色 / 刻度 · storage key           | ✓                        | D01–D14 · D08         |
| 落点纪律：取数 · 副作用 · 状态 · 守卫 · 环境读取         | ✓                        | S36 S38 S41 S42 S44   |
| 文案（裸文案 / 键存在 / 死键 / 分片聚合 / 语言对账）     | ✓                        | C01–C07               |
| 依赖选型（白名单 / 未用 / 幽灵 / 适配器对账 / 手搓轮子） | ✓                        | P01–P12               |
| 生成物标记 · 假异步 · 假数据 · legacy · 脱离上下文 API   | ✓                        | H06–H13               |
| 度量（覆盖率 / 预算 / 链路）                             | ✓                        | M02–M09               |
| **单一出处：后端端点**                                   | **✗ 无**                 | ——                    |
| **缓存键形状一致（前缀规约）**                           | **✗ 无**                 | ——                    |
| **DTO ↔ 领域模型映射**                                   | **✗ 无**                 | ——（判据偏建模）      |
| **错误 / 加载态**                                        | **✗ 无**                 | ——（形态自由度大）    |
| **状态重复持有 / 组件直读全局 store**                    | **✗ 无**                 | ——                    |
| **多落点单一出处**（键跟着模块走）                       | **✗ 能力不支持**         | `queryKeyFrom` 是单值 |
| FSD **`@x` 跨引用公开面**                                | **✗ 未实现**（误报 S01） | 官方规范的一等公民    |

## 6. 已确认的架构决策（用户 2026-09-25 定）

| #   | 决策                                  | 含义                                                                                                                                                                                      |
| --- | ------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| ①   | **取数归域/实体持有**（不统一到页面） | canonical 现状保持（取数在 `modules/<域>/hooks`）；FSD 从 `pages/*/api` 下沉到 `entities/*/api`（或 `features/*/api`）；hook **返回领域实体**而不是 `string[]`；补明细 → 领域模型的映射层 |
| ②   | **缓存键跟着模块/切片走**（不集中）   | `queryKeyFrom` 要支持多落点（glob/数组）；键文件落在 `modules/<域>/model/keys.ts` / `entities/<e>/model/keys.ts`；跨域用键走公开面                                                        |

这两条同时解释了 C1（数据契约集中在 shared）与 C2（跨域没有合法通道）—— **它们不是示例的疏忽，而是当前能力把项目推向了 shared**。

## 7. 从「确认好的架构」倒推：要开发什么

分三类。**A 类是示例重构**，**B 类是本体能力/规则**，**C 类明确不做**。

### A. 示例要改（照抄会长歪的形状）

| #   | 改什么                                                                                                               | 依据       |
| --- | -------------------------------------------------------------------------------------------------------------------- | ---------- |
| A1  | FSD：`pages/*/api` → `entities/<e>/api`（含 DTO、mapper、keys、policy）；`fetchIn` 加 `src/entities/*/api/**`        | 决策①      |
| A2  | 两边：hook 返回领域实体 + 显式 mapper（`toCrew(dto): Crew`）；`CrewCard`/`OrderCard` 收实体而不是裸值                | 决策① · C5 |
| A3  | 两边：缓存键文件按域/实体拆（`modules/<域>/model/keys.ts`）；键与请求**同源**（C10 一并修）                          | 决策②      |
| A4  | canonical：给域一个**业务公开面**（`modules/<域>/index.ts`，入口角色），并真演示一次跨域协作（app 组装层粘合）       | C2（高）   |
| A5  | 两边：埋点改成 `useTrackView(event)`（内部 `useEffect`），`sendEvent` 不再对外导出                                   | C3（高）   |
| A6  | canonical：加样式入口 `src/app/styles/index.css`（`@import` 四份）并在 `main.tsx` 引入；两个示例的主题/令牌要真生效  | C6         |
| A7  | 两边：`theme.css` 增 `--on-brand`（明暗各一份），组件改用它；`contrastPairs` 改成品牌底那一对                        | C7         |
| A8  | 两边：i18n 补复数（en `_one/_other`，zh `_other`）；命名空间与切片对齐                                               | C8         |
| A9  | 两边：全局状态收窄为"真正跨域且域无关"的（其余进域 model）；S41 的 `in` 相应放宽                                     | C9         |
| A10 | canonical：`PATHS` 加参数化模式 + 构造器（`crewsDetail: '/crews/:id'` + `crewDetail(id)`），示例演示一个 detail 路由 | C11        |
| A11 | FSD：`crew-filter` 只做受控筛选，列表渲染提到 `widgets/crew-list`；`model` 补纯函数                                  | C14        |
| A12 | FSD：补 `pages/login`（或注明"登录页由宿主补"）                                                                      | C15        |
| A13 | 两边：注释/保留名/事件名对齐（`common`、`index.html` 注释、事件双名、FSD 配置注释）                                  | C13        |
| A14 | 两边：测试换成行为测试；`requireTestsFor` 扩到域/切片的 `lib`、`model`                                               | C12        |

### B. 本体要开发（能力 / 规则）—— 按证据强度排序

| #   | 开发什么                                                                                                          | 证据                               | 前置工作                                                                                       |
| --- | ----------------------------------------------------------------------------------------------------------------- | ---------------------------------- | ---------------------------------------------------------------------------------------------- |
| B1  | **`queryKeyFrom` 支持多落点**（glob/数组）                                                                        | 决策②（用户已确认）                | 无（规则读落点的形态改一下 + R-86 自述要按落点逐个报 0 命中）                                  |
| B2  | **域/切片级业务公开面**（canonical 的 `modules/<域>/index.ts` 作为 `entry`）——否则跨域协作只有"抬进 shared"一条路 | C2（高）                           | 预设角色表 + structure 默认；与 S23/S03 联动                                                   |
| B3  | **后端端点唯一出处**（新受体 + 规则）                                                                             | §3 盲区实测                        | **扩事实模型**：记录实参形态 / 模板静态段                                                      |
| B4  | **缓存键形状一致**（同一 key 工厂的数组首元素必须同前缀）                                                         | §3 盲区实测（`invalidate` 不失效） | **扩事实模型**：数组首元素带所属导出+属性路径                                                  |
| B5  | **埋点调用位置**（`sendEvent` 不许在 render 体直接调）                                                            | C3（高，两边都犯）                 | 事实里已有 `functions`/`hasJsx`，判据是"调用是否在组件体而非 hook/effect 内"——需先验证可判定性 |
| B6  | **对比度组合覆盖**：组件样式里出现的 `(color, background)` 令牌组合必须在 `contrastPairs` 里声明                  | C7（门禁抓不到）                   | 事实模型已有 `styleProps` + `css` 解析；可判定                                                 |
| B7  | **样式入口**：声明了令牌/全局样式目录，就必须有文件被引用（否则主题静默不生效）                                   | C6（"绿但坏"）                     | 图 + 样式落点，可判定                                                                          |
| B8  | **i18n 复数**：`_one/_other` 形态的键要能过 C03（两语言键全等），并演示                                           | C8                                 | C03 的判据要加复数后缀豁免                                                                     |
| B9  | **`@x` 跨引用公开面**（官方一等公民）                                                                             | C16                                | 角色表 + S22 例外                                                                              |
| B10 | **配置入口三件**：报告告诉你"补哪一行" / 报告给可复制片段 / `init` 生成器                                         | 用户提问"用户会配吗"               | 前两件是报告层改造，第三件是新 CLI 入口                                                        |

### C. 明确不做（建议进 `REQUIREMENTS.md` §八，附理由）

| #   | 不做什么                        | 理由                                                                                                     |
| --- | ------------------------------- | -------------------------------------------------------------------------------------------------------- |
| C-1 | **错误 / 加载态的形态**         | 形态自由度大：`{data,isLoading}` / `Result` / 抛异常 / 状态机都合理，判了就是误报（N-09/N-11/N-12 同类） |
| C-2 | **DTO ↔ 领域模型的映射约定**    | 是建模选择，不是可判定不变量；**示例里加 mapper**（A2）但本体不立规则                                    |
| C-3 | **错误被吞掉 / try-catch 形态** | 同上                                                                                                     |
| C-4 | **`@x` 之外的跨切片自由互引**   | 与 isolate 冲突；真要放开的只有官方 `@x` 这一条（B9）                                                    |
