# 需求

> **这份文档是需求的唯一来源。**
> 要做什么、做没做、**这条该由谁来做**（本体还是委托给 eslint / stylelint / knip 这类工具），都看这里；
> 不再散落在对话里。**委派出去的检查本身就是需求**：交给谁写在它自己的行上（`状态` + `交给谁`）。
> 而"约束 → 原规则 id → 生态的哪条规则"那套**设计映射**在设计文档里（`docs/DESIGN.md` §4.9），
> 两处的委派口径由 `tests/process.test.mjs` 对齐。
> 每条需求都带一个**具体例子**（哪个文件、哪段代码、出了什么事）；没有例子的需求等于没写。
> 这里不写实现方案：规则编号、配置字段、判定细节在 `docs/DESIGN.md` 与 `.scratch/<slug>/spec.md`。

## 怎么用

- 每条 = **长这样**（具体代码/文件）+ **会怎样**（不用它会出什么事）+ **期望**（该由谁拦、拦到什么程度）。
- 标题行上是 **状态** 与 **交给谁**：一条需求归本体还是能委托出去，就在它自己那一行上。
- 新需求先写进这份文档，再开工；做完把状态改成 `已完成`（或 `已委派`），并在 CHANGELOG 记一笔。
- **编号不复用、不重排**：撤掉的需求把编号登记在第八节（写成「原 R-xx」），正文里不再出现 ——
  这样 `spec.md` 里的 `R-xx` 引用永远有效，流程门禁会校验「缺的编号必须被声明为已撤」。
- **编号**：`R-01…R-53` 按需求出现顺序编；**R-54 起是拿全量规则清单做对账后补齐的**
  （原先只列了对话里出现过的场景，漏了「已实现但没人提」的那批）。正文按主题分组，编号不随主题重排。
- **写不出真实场景的需求先不做**（约定见 `docs/agents/scenario-first.md`）。
- 底线：**宁可漏报，不可误伤** —— 正常写法被报红一次，整条门禁就没人信了。

**状态**

| 状态       | 含义                                                  |
| ---------- | ----------------------------------------------------- |
| `进行中`   | 有规格、正在做                                        |
| `未开始`   | 判据清楚、已排期，可以直接开工                        |
| `需定口味` | 判据还不清楚，得先有人拍板                            |
| `待定`     | 还没决定做不做（或本期范围外）                        |
| `已完成`   | 本体已实现，有夹具与测试                              |
| `已委派`   | 交给生态工具（本体不做）—— **宿主得装了才有这层覆盖** |
| `不做`     | 明确不做，别再讨论                                    |

**交给谁（能不能委托出去）**

判据只有一条：**纯语法 / 纯图属性，且不需要项目专有数据 → 可以委托；需要角色表、适配器、
方案表、项目声明 → 只能本体做**（生态工具不知道你项目的目录契约长什么样）。

| 交给谁              | 含义                                                                            |
| ------------------- | ------------------------------------------------------------------------------- |
| `本体`              | 需要项目专有数据（契约 / 声明 / 适配表），生态工具做不了                        |
| `可委派：eslint`    | 纯语法，eslint 有现成规则                                                       |
| `可委派：stylelint` | 纯 CSS 语法，stylelint 有现成规则                                               |
| `可委派：knip`      | 依赖与导出的"声明了没用 / 用了没声明"                                           |
| `可委派：dc`        | dependency-cruiser：纯图属性（环、孤儿）                                        |
| `可委派：外部工具`  | 不是 lint（如公开面快照用 api-extractor 类）                                    |
| `混合`              | 一半生态、一半本体（例：色值唯一出处要令牌图 → 本体；`!important` → stylelint） |
| `生态无现成规则`    | 想委派也没人做，只能本体（或自写插件，不划算）                                  |

> **委托的前提**：`已委派` 不等于"有人在管" —— 宿主没装那个工具就等于失去那层覆盖。
> 所以"委派出去的检查到底跑没跑"这件事，本工具**不判**（曾经规划过 R-51，已判不做）——委派就等于把覆盖责任交给宿主。

---

## 一、每天都会踩的：结构与引用

**R-01 文件放错地方** · 已完成 · 本体（要目录契约）

- **长这样**：`src/modules/crews/utils/format.ts`（域里没有 `utils` 这个槽位）、`src/modules/crews/views/types.ts`（类型塞进页面目录）
- **会怎样**：目录契约只在 review 里口口相传，过几周没人说得清该放哪；新人各放各的
- **期望**：不在契约里就报，并且**告诉它该放哪**（`model/` / `lib/` …），不是只说"你错了"

**R-02 跨域直接抓别人的内部文件** · 已完成 · 本体（要域划分）

- **长这样**：`modules/crews/routes.ts` 里 `import { OrdersTable } from '@/modules/orders/components/OrdersTable'`
- **会怎样**：orders 一改这个组件，crews 就崩，"域是自治单元"变成口号
- **期望**：跨域只能 `import '@/modules/orders/routes'`；直接引对方内部文件报

**R-03 循环依赖** · 已完成 · 可委派：dc（本体也做了）

- **长这样**：`crews/model/selectors.ts` → `orders/model/selectors.ts` → 又回 `crews/model/selectors.ts`
- **会怎样**：其中一个在初始化时拿到 `undefined`，报错栈指向完全无关的地方，排查半天
- **期望**：依赖图里出现环就报，锚在环里最容易定位的文件上
- **备注**：dependency-cruiser 的 `no-circular` 能做，但宿主得装并配；本体反正已经算好了图，顺手判更稳

**R-04 导入路径写错** · 不做（委派） · 可委派：eslint

- **长这样**：`import { format } from '../../../lib/format'`（少写了一层 `..`）
- **会怎样**：解析不到，但工具什么都不说；直到运行时白屏
- **期望**：交给 eslint `import/no-unresolved`（宿主本来就有 eslint，编辑器和编译器也会先喊）。
  **本体原先自己实现（S33），0.4.0 移除** —— "路径写错"是编辑器/编译器就该说的错，不值得占目录契约的一条红线；
  引擎的事实 `graph.unresolved` 保留（依赖图要用）

**R-05 底层反过来依赖上层** · 已完成 · 本体（要层号）

- **长这样**：`src/shared/lib/format.ts` 里 `import { crewsRoutes } from '@/modules/crews/routes'`；或域里 `import { routes } from '@/app/router'`
- **会怎样**：共享层要跟着业务改；拆域时发现共享层才是最底层动不了的地方
- **期望**：依赖只能单向，反向引用报

**R-06 死代码 / 死切片** · 已完成 · 混合（文件级可委派 knip，组级只能本体）

- **长这样**：`shared/lib/format-old.ts` 没有任何 import；整个 `orders/` 域没有任何域外引用
- **会怎样**：越堆越多，谁都不敢删；新人照着死代码改
- **期望**：从入口不可达的文件报；整个组没有任何外部引用（死切片）报

**R-07 页面访问不到** · 已完成 · 本体（要槽位）

- **长这样**：`modules/crews/views/CrewsPage.tsx` 写好了，但 `crews/routes.ts` 里没有引用它；或者有 `views/` 却压根没有 `routes.ts`
- **会怎样**：页面永远打不开，上线才发现
- **期望**：有页面却缺域入口报；页面没被本域入口引用报

**R-08 目录与命名的规矩** · 已完成 · 本体（要角色表）

- **长这样**：同层里 `crews-page` / `orders-page` / `user-page`（都在重复 `page`）；`page` 与 `pages` 混着用；一个域目录下 30 个子项；片段里又出现 `lib/`
- **会怎样**：每次 review 都要争这些；命名各写各的，看不出层级
- **期望**：深度、槽位、保留名、组名重名、重复词、单复数、组数与目录子项上限，超了报

**R-09 入口文件的叫法** · 已完成 · 本体（要项目声明）

- **长这样**：项目用配置式路由，域入口是 `modules/crews/routes.ts`（不是 `.tsx`）；后来改名成 `router.ts` 但契约没跟着改
- **会怎样**：前者被误报"跨域引用了内部文件"；后者入口彻底没人管（规则看不见它）
- **期望**：入口叫法由项目声明、规则跟着走；声明了却没在契约里登记，明确报"契约没跟上"

**R-14 假异步 / 假数据** · 已完成 · 本体（0.4.0 收回 H07/H09；裸 `Math.random` 判不了，见 N-08）

- **长这样**：`await new Promise((resolve) => setTimeout(resolve, 300))` 当请求用、`const mockUsers = [...]` 留在生产路径
- **会怎样**：假实现混进真实路径；测试与线上行为不一致
- **期望**：`new Promise` 与 `setTimeout` 同时出现就提示（H07，warn，"疑似"只提示不拦）；非测试代码里文件 / 导出名带 `mock` / `fake` / `dummy` 就提示（H09，warn）。
  裸 `setTimeout` 不判（防抖 / 轮询是正当用法）；`Math.random` 判不了（产生随机数不一定是错）—— 原先委派 eslint `no-restricted-syntax`，0.4.0 收回本体

**R-15 硬编码地址** · 已完成 · 本体（0.4.0 收回 H08）

- **长这样**：`const BASE = 'http://localhost:3000'`、`'http://192.168.1.20/api'` 写死进源码
- **会怎样**：环境切不过来；本地 / 内网地址跟着构建进生产
- **期望**：**地址进环境变量**（`import.meta.env.VITE_API_URL` / `.env` / 构建配置），源码里不出现本机 / 内网地址；
  真出现了就报（H08）：`localhost` / `127.0.0.1` / `0.0.0.0` / `[::1]` / `10.x` / `192.168.x` / `172.16–31.x`。
  **判据不为"本地调试"放宽**（写 localhost 是常态，但那是环境变量该管的事）：dev-only 形态（Storybook 故事、dev-only 模块）
  登记成非生产角色即放行，确实要留的本地兜底值走规则级例外 —— 两条出口都是**宿主数据**，见 `PARADIGM.md` §7.1。
  **外部地址不判**（`https://api.example.com`、文档链接）—— 换域名不是本工具管的事；测试文件里的 `localhost` 是 mock 服务，不判。
  原先委派 eslint `no-restricted-syntax`（要宿主自己写 esquery 选择器），0.4.0 收回本体

**R-16 相对越级 `../`** · 已完成 · 本体（0.4.0 收回）

- **长这样**：`import { format } from '../../../../shared/lib/format'`
- **会怎样**：看不出这条依赖跨没跨界（同层？跨域？跨层？）；目录一挪就全崩
- **期望**：声明"相对路径最多向上爬几层"（`structure.maxRelativeUp`）后，爬过上限就报；不声明不判。
  原先委派给 eslint `no-restricted-imports` —— patterns 要项目自己写，而"几层算越级"因项目而异

**R-54 语法错误的文件必须报出来** · 已完成 · 本体（解析失败只有本体知道）

- **长这样**：`CrewsPage.tsx` 里少了一个 `}`，整个文件解析不了
- **会怎样**：这个文件失去**全部**检查（角色、命名、依赖、文案…一条都不跑），门禁却照样绿
- **期望**：解析失败的文件直接报出来（fail-closed），并说明它因此失去了哪些检查

**R-55 再导出满天飞（barrel）** · 已完成 · 本体（要目录契约）

- **长这样**：`shared/components/index.ts` 里 `export * from './ui/Button'`、`export * from './common/Card'`…
- **会怎样**：改一个文件的影响面不可知；打包器摇不掉没用的代码；循环依赖更容易冒出来
- **期望**：`export *` 报；要开放面就显式列出导出

**R-56 导出形态契约** · 已完成 · 本体（要槽位语义）

- **长这样**：`views/CrewsPage.tsx` 只做了命名导出；`hooks/useCrews.ts` 顺手导出一个 `crewsApi`；`model/types.ts` 里导出一个函数
- **会怎样**：页面懒加载拿不到组件；hook 目录混进非 hook；model 层混进逻辑
- **期望**：页面必须 default 导出；hooks 只导出 `use*`；model 只放类型与字面量常量；`lib/` 里不许有 JSX

**R-57 同一个导出名在两处定义** · 已完成 · 本体（要目录表）

- **长这样**：`crews/model/format.ts` 与 `orders/lib/format.ts` 都导出 `formatCrew`
- **会怎样**：两份实现慢慢分叉，修了一处漏一处
- **期望**：同名导出在多处定义报（能证明"两份实现"确实存在）

**R-58 单文件导出太多 / 组件太多** · 已完成 · 本体（要阈值）

- **长这样**：一个文件导出 20 个值；或者一个文件里塞了 5 个组件
- **会怎样**：这个文件变成「什么都往里放」的入口，改哪儿都要动它
- **期望**：按声明的阈值报。**库范式不判** —— 库的入口就是公开面，导出多是正常形态

**R-59 项目里混进框架包量不了的源码** · 已完成 · 本体（要框架包）

- **长这样**：React 项目里出现 `src/components/Chart.vue`
- **会怎样**：这个文件被**静默跳过**（扫描不认它的扩展名），表现成「0 个文件 → 通过」
- **期望**：出现当前框架包量不了的源码就直接报（换包，或把这段源码移出扫描范围）

**R-60 `shared` 里的东西只被一个域用** · 已完成 · 本体（要图与域划分）

- **长这样**：`shared/lib/crew-format.ts` 只有 `crews` 一个域 import
- **会怎样**：共享层堆着一批「其实是某个域私有」的东西，谁都不敢动
- **期望**：提示它该下沉到那个域（是提示，不是红线）

**R-61 空壳组** · 已完成 · 本体（要组维度）

- **长这样**：`features/crews/index.ts` 只有一行 `export {}`，没有任何实现文件
- **会怎样**：目录看起来做了模块化，其实是空壳；读代码的人被目录骗
- **期望**：声明了组维度后，「只有入口、没有任何片段」的组报

**R-62 扫描域配错，一个文件都没匹配到** · 已完成 · 本体（只有门禁自己能查）

- **长这样**：配置里把扫描域写成 `app/**`，而项目源码在 `src/**`
- **会怎样**：0 个文件参与判定，报告显示「通过」 —— 最危险的假绿
- **期望**：扫描域非空却一个源码文件都没匹配到，直接报

**R-63 导入风格不统一** · 已完成 · 本体（要组维度，声明才判）

- **长这样**：同一个切片里 `import { x } from '@/features/crews/model/x'`（该用相对）；跨切片 `import { y } from '../../orders/model/y'`（该用别名）
- **会怎样**：看不出这条依赖到底跨没跨界
- **期望**：同组内必须相对、跨组必须走别名（**声明了才判**，与社区 linter 一样默认关）

**R-64 同层切片互相直接引用** · 已完成 · 本体（要组维度，声明才判）

- **长这样**：`features/crews/ui/Card.tsx` 里 `import { ordersModel } from '@/features/orders/model'`
- **会怎样**：切片之间绑死，「切片可独立演进」是假的
- **期望**：同层不同切片之间不许互相引用（声明了才判）；要复用就走公开面或提升到 shared

## 二、换库 / 换方案

**R-17 幽灵依赖 / 声明了没用** · 已完成 · 本体（0.4.0 从 knip / depcheck 收回）

- **长这样**：import 了从没写进 `package.json` 的包（靠别人的依赖碰巧能跑）；`dependencies` 里躺着早就没人 import 的库
- **会怎样**：换机器 / CI 就崩；依赖清单越滚越大，没人敢删
- **期望**：**两条分开判** —— 幽灵依赖（import 了没声明）报在**引用它的那一行**（error）；声明未用（装了没人 import）报在 `package.json` 上（warn）。
  0.4.0 收回本体：事实早就算好（`ctx.deps.phantom` / `unused`，`--verify-deps` 一直在用），
  而生态侧要装 knip/depcheck 或 eslint-plugin-import 并写配置才有覆盖 —— 配置成本高到多数宿主不会做

**R-18 路由路径手写** · 已完成 · 本体（要项目声明的唯一出处）

- **长这样**：`<Link to="/orders">`、`navigate('/crews')`、`{ path: '/crews' }` 散在十几个文件里；唯一出处 `shared/config/paths.ts` 反而没人用
- **会怎样**：改一个路径要全仓搜，漏一处就是 404（还没人发现）
- **期望**：路径只许出现在声明的唯一出处，别处报

**R-19 缓存键手写** · 已完成 · 本体（同上）

- **长这样**：页面里 `useQuery({ queryKey: ['crews', id] })`，另一处 `['crews', 'list']`，还有一处 `['crew', id]`（少个 s）
- **会怎样**：缓存不命中、数据不刷新，且极难查（键是字符串，编译器不管）
- **期望**：键只许出现在声明的唯一出处，别处报

**R-20 页面里取数 / 域里直连后端** · 已完成 · 本体（要项目声明"取数只许在哪"）

- **长这样**：

  ```tsx
  // modules/crews/views/CrewsPage.tsx —— 页面直接取数
  export default function CrewsPage() {
    const { data } = useQuery({ queryKey: crewsKeys.list, queryFn: loadCrews })
    return <div>{data?.length}</div>
  }
  ```

  ```ts
  // modules/crews/lib/api.ts —— 域里直连后端，还手写了一份 DTO
  export async function loadCrews() {
    const res = await fetch('/api/crews')
    return (await res.json()) as CrewDto[]
  }
  ```

- **会怎样**：换数据层要翻遍页面；`CrewDto` 这样的契约类型散在十几个域里，没有一处是"后端的真身"；测试必须 mock 网络
- **期望**：取数只许出现在项目声明的那几处（域内 hooks / 共享 api 层）；页面里取数、域里直连后端都报。**测试文件不报**
- **备注**：理论上能写成 eslint 的选择器，但"落点在哪"是项目专有 —— 写进 eslint 就是把契约搬进另一个工具

**R-21 埋点上报 / 本地存储散在各处** · 已完成 · 本体（同上）

- **长这样**：组件里 `localStorage.getItem('token')`、`gtag('event', 'crews_view')`，hook 里 `Sentry.captureException(error)`；而封装 `shared/lib/storage.ts` / `analytics.ts` 只有一半调用走
- **会怎样**："用户没同意隐私协议就别上报"没地方统一；要给 token 加密、加存储版本迁移时全仓找，漏一处就是半加密状态；换埋点 SDK 要动十几个文件
- **期望**：埋点上报与本地存储只许出现在项目声明的封装里，别处报。测试文件不报

**R-22 域里自建配置对象（双实例）** · 已完成 · 本体（同上）

- **长这样**：

  ```ts
  // app/main.tsx —— 应用一份
  export const queryClient = new QueryClient()

  // modules/crews/lib/queryClient.ts —— 这个域又建了一份
  export const crewsClient = new QueryClient()
  ```

- **会怎样**：两个缓存实例 → 保存成功后 `invalidateQueries` 不生效、数据不刷新，查一下午；主题/状态容器同理
- **期望**：这类配置对象只许在装配层或共享层建，域里自建报

**R-23 同类方案混用** · 已完成 · 本体（要方案表）

- **长这样**：方案表登记的是 react-query，某个新页面里 `import useSWR from 'swr'`
- **会怎样**：两套缓存并存，缓存策略、失效逻辑各一半；换库换不干净
- **期望**：登记了某个方案后再引入同类里的另一个库，报

**R-24 声明与实际依赖不一致** · 已完成 · 混合（按方案表对账 → 本体；其余见 R-17）

- **长这样**：方案表里写着 `@tanstack/react-query`，但 `package.json` 里没有；或者装了 `antd` 而方案表登记的是另一套组件库
- **会怎样**：本地能跑（因为某个依赖间接带了它），换机器/CI 就崩；两套组件库同时存在，样式互相打架
- **期望**：声明了要装就必须装；装了方案表之外的同类库报；开白名单时未登记的运行时依赖报

**R-25 样式方案换了形态** · 已完成 · 本体（要项目声明的形态）

- **长这样**：项目从 CSS Modules 换成 Sass，组件样式文件变成 `Card.module.scss`；或者用 Tailwind，项目里根本没有 `.module.*` 文件
- **会怎样**：前者被当成"组件目录里裸放的全局样式"误报一片；后者被要求写一个不存在的文件
- **期望**：组件样式文件的形态跟着项目声明走；确实没有这种文件的方案，规则**不判**（而不是乱报）

**R-65 图标只能来自登记的包** · 已完成 · 本体（要适配表）

- **长这样**：适配表登记的是 antd 图标，某个文件里 `import { FiUser } from 'react-icons/fi'`
- **会怎样**：图标风格与体积失控；换库时漏掉一处就是残留
- **期望**：图标 import 只许来自适配表登记的图标包

**R-66 项目明令禁止的库** · 已完成 · 本体（要 deny 清单）

- **长这样**：配置里禁用 `axios`，但 `dependencies` 里躺着它
- **会怎样**：禁令只写在文档里，代码照用
- **期望**：装了被明确禁用的库就报（这是白名单之外的第二种表达方式）

## 三、上生产前

**R-26 首屏把所有页面都打进去** · 已完成 · 本体（要"页面"槽位与入口）

- **长这样**：

  ```ts
  // modules/orders/routes.ts —— 静态 import，页面随主包一起下载
  import OrdersPage from './views/OrdersPage'
  export const ordersRoutes = [{ path: '/orders', element: <OrdersPage /> }]
  ```

- **会怎样**：所有页面进主包，首屏 1.2MB，第一个月没人发现（本地开发感觉不到）
- **期望**：项目声明"页面要懒加载"后，入口对页面的静态 import 报；改成 `lazy: () => import('./views/OrdersPage')` 即为合规

**R-27 测试与覆盖率的退步** · 已完成 · 本体（要覆盖率产物与棘轮）

- **长这样**：这次改了 `modules/crews/hooks/useCrews.ts`，没补测试；覆盖率从 82% 掉到 79%；CI 里用的还是上周生成的覆盖率产物
- **会怎样**：门禁拿着旧数据说"通过"；覆盖率缓慢下滑没人拦
- **期望**：覆盖率不许倒退；本次改动的文件必须被覆盖；产物过期报

**R-28 门禁自己漏跑** · 已完成 · 本体（只有门禁自己能查）

- **长这样**：有人把 `test` / `coverage` 从 `pnpm check` 链路里摘了；或者覆盖率一直在跑，但统计的是错的进程（本仓真发生过）
- **会怎样**：门禁显示绿色，其实什么都没跑 —— 这类"假绿"只有门禁自己能查
- **期望**：声明的提交链路里必须真的跑测试与覆盖率，缺了就报

**R-29 文件 / 函数行数失控** · 已完成 · 混合（eslint + 本体）

- **长这样**：一个 `CrewsPage.tsx` 涨到 800 行；一个函数 200 行没人敢动
- **会怎样**：改一处要读半天；review 时只能"看不下去了"才发现
- **期望**：本体按项目声明的阈值判；eslint 侧也能配同一套上限

**R-67 新加的文件没有测试** · 已完成 · 本体（要测试约定）

- **长这样**：新增 `modules/crews/hooks/useCrews.ts`，仓库里没有对应的 `useCrews.test.ts`
- **会怎样**：覆盖率被别的文件撑着，新代码其实没人测
- **期望**：项目声明「哪些文件必须配测试」后，缺测试文件报

**R-68 依赖数量悄悄变多** · 已完成 · 本体（要预算）

- **长这样**：半年里运行时依赖从 3 个涨到 11 个，每一次都说「就加一个包」
- **会怎样**：体积、审计、升级成本都在涨，但没有任何一次改动「看起来是错的」
- **期望**：项目声明依赖预算后，超了报（与依赖白名单是一件事的两面）

## 四、文案 · 设计系统 · 反退化

**R-31 文案键** · 已完成 · 本体（要 locales 目录与语言声明）

- **长这样**：`locales/zh-CN/nav.ts` 里有 `crews`，`locales/en-US/nav.ts` 没有；页面删了但 `nav.crews` 还留着没人用；新加了 `locales/zh-CN/order.ts` 但没有被 `i18n/index` 聚合
- **会怎样**：英文界面显示键名；语言目录越走越不一致；删不掉的死键
- **期望**：键缺语言、目录不同构、死键、声明了却没有资源，都报

**R-32 裸文案（界面里写死）** · 已完成 · 本体（0.4.0 收回，三种形态）

- **长这样**：`<button>保存</button>`、`<input placeholder="搜索" />`、`message.success('保存')`、`notification.open({ message: '保存' })`
- **会怎样**：这句文案永远翻译不了；上线后才发现界面上冒出中文
- **期望**：声明了 i18n 才判，命中给 warn（C01）—— JSX 文本节点、面向用户的属性（`title` / `placeholder` / `alt` / `aria-label` / `label`）、
  以及声明的组件库调用里的文案（`messageApis` 声明调用、`messageProps` 声明哪些属性算文案）。
  原先委派 `eslint-plugin-i18next` 的 `no-literal-string`（要宿主装插件 + 维护 ignore），0.4.0 收回本体

**R-33 颜色与令牌** · 已完成 · 本体（色值唯一出处 / 令牌图 / 对比度 / `!important` / 数值白名单，0.4.0 全部收回）

- **长这样**：`Card.module.css` 里 `color: #ff5a1f`；`var(--sh-brand)` 在没人定义的地方被引用；`--sh-static-gray-3` 定义了没人用；暗色主题少了一个令牌；正文对比度 3.1；业务样式里出现 `.ant-table-cell`
- **会怎样**：换主题漏改、深色模式花掉、换组件库残留旧库样式；视觉不一致没人能一眼看出来
- **期望**：色值只在色板、令牌引用闭合、无死令牌、明暗同名、对比度基线、第三方类名不许出 vendor 目录，都报

**R-34 颜色字面量只在色板** · 已完成 · 本体（0.4.0 收回）

- **长这样**：样式文件里直接写 `#ff5a1f`、`rgba(0,0,0,.5)`
- **会怎样**：同一套设计出现三种近似色；换品牌色要全仓搜
- **期望**：色板 / 令牌目录之外出现颜色字面量就报（D01 —— 目录来自 `designSystem()` 的 params，不用宿主再维护一份白名单）；色板里只许放静态令牌（D02）。
  原先委派 stylelint `color-no-hex` + overrides，0.4.0 收回本体

**R-35 `!important`** · 已完成 · 本体（0.4.0 收回）

- **长这样**：`color: red !important`（用来压过别人，而不是改对层级）
- **会怎样**：样式权重失控，后面的人只能加更强的 `!important`
- **期望**：样式里出现 `!important` 就报（D09），但**声明的 vendor 目录**是正当出口（覆盖第三方样式总得有地方写）。原先委派 stylelint `declaration-no-important`，0.4.0 收回本体

**R-36 长度 / `z-index` / 时长必须走令牌** · 已完成 · 本体（0.4.0 收回，声明白名单才判）

- **长这样**：`margin: 13px`、`z-index: 9999`、`transition-duration: 200ms` 直接写数字
- **会怎样**：间距、层级、节奏各写各的；视觉细节无从统一
- **期望**：长度 / `z-index` / 时长三类数值必须在**声明的刻度白名单**里（D12–D14）；不声明白名单就不判。原先委派 stylelint 的值白名单（要逐属性列），0.4.0 收回本体

**R-37 退路留在仓库里** · 已完成 · 生态无现成规则（只能本体）

- **长这样**：重构后 `views/CrewsPage.old.tsx`、`hooks/useCrewsLegacy.ts` 还在；而 `shared/lib/legacy-support.ts`（给旧浏览器兜底的正常模块）不该被牵连
- **会怎样**：没人敢删；新人抄了旧的那份，两套实现行为不一致
- **期望**：文件名带"旧实现"整段标记的报；正常模块与迁移目录不误伤
- **备注**：eslint 没有"文件名"类现成规则，要么自写插件（不划算），要么本体顺手做

**R-38 换库没换干净** · 已完成 · 本体（要"本项目登记的是哪套"）

- **长这样**：从 antd 换到 MUI，样式里还留着 `.ant-table-cell`；代码里出现 `@apply`（旧框架语法）
- **会怎样**：两套样式互相打架；旧库残留没人清理，只会在出 bug 时被发现
- **期望**：残留报

**R-69 组件库的静态 API 不走上下文** · 已完成 · 本体（要适配表）

- **长这样**：`message.success('已保存')`、`Modal.confirm(...)`（静态方法拿不到主题与 i18n 上下文）
- **会怎样**：弹窗没有主题色、文案不能翻译；「为什么这个提示长得不一样」每次都要重新查
- **期望**：适配表登记的这类全局 API 必须走上下文用法（如 `App.useApp()` 拿 message / notification / modal）

## 五、治理：要能解释、能追责

**R-39 规则没跑要明说** · 已完成 · 本体（工具自身的自述能力）

- **长这样**：项目没配 i18n，报告里显示"通过"，其实是 5 条文案规则压根没跑
- **会怎样**：团队以为"文案这块有人管"，其实是没人管 —— 最危险的静默失效
- **期望**：没跑的规则在报告里**逐条列出停用原因**

**R-40 生效的是哪套方案要看得见** · 已完成 · 本体

- **长这样**：配置里两套路由方案同时写着，跑完只看到"通过/失败"
- **会怎样**：换库换错了、组合配置手滑多写一份，现场完全看不出来，只能翻配置文件
- **期望**：每次运行自述"生效的这几套方案"；要字段级全貌时有一条命令列全表

**R-41 同一个方案被声明两次** · 已完成 · 本体

- **长这样**：组合配置里已经装了一套组件库，之后又手写了一份另一套
- **会怎样**：悄悄取后者生效；查"到底哪套在跑"要读完整份配置
- **期望**：内容不同的两份声明直接报错（要覆盖走显式覆盖入口），不静默取后者

**R-42 豁免要有代价** · 已完成 · 本体

- **长这样**：豁免里某条只写规则和文件、不写理由；或者写了到期日，早就过期但没人管
- **会怎样**：曾经的"一键重写基线"让"重写"永远比"修"便宜，门禁退化成"没有新增违规"
- **期望**：豁免必须写理由、可设到期日，**过期即红**；不提供一键重写基线的退路

**R-43 手搓轮子** · 已完成 · 本体（指纹表 + "同名"要一起看，自写选择器易误伤）

- **长这样**：`lib/debounce.ts` 里手写 `setTimeout` + `clearTimeout`，并且导出的函数就叫 `debounce`
- **会怎样**：少了取消、立即执行、参数转发这些边界；出了 bug 才发现和成熟库行为不一致
- **期望**：形态证据 + 与成熟库 API 同名**两条同时成立**才提示（单条不报，避免误伤正常写法）

**R-71 适配表声明的库必须真的被用** · 已完成 · 本体（要适配表与图）

- **长这样**：适配表登记了 antd，但全仓没有一处 import antd，也没有一处 `.ant-` 样式
- **会怎样**：以为「用了组件库、规则在管」，其实依赖它的那几条规则一条都没东西可判（静默空转）
- **期望**：声明了就必须真的被用；否则把适配表改成「不用」，或删掉那一行

**R-72 配了设计系统就必须真有令牌文件** · 已完成 · 本体（要声明与磁盘对账）

- **长这样**：配置里加了设计系统，但 `paletteFile` 指向的文件不存在、令牌目录是空的
- **会怎样**：颜色 / 令牌 / 对比度那整套等于没跑，报告却显示「通过」
- **期望**：声明与磁盘事实对账，对不上就报

## 六、委派出去的（本体不做，交给生态）

> 这些约束**由生态承担**（约束 → 规则 → 生态规则的映射见 [`docs/DESIGN.md`](docs/DESIGN.md) §4.9）：
> 宿主装了对应工具才有这层覆盖，而"跑没跑"本工具不判（R-51 已判不做）。
> **共同点：开箱即用或一行配置就能开**，所以本体不重复实现 —— 需要项目写声明的那些，
> 已在 0.4.0 收回本体（见第一~五节里对应的条目）。

**R-10 `any` / 非空断言 / 逃生舱注释** · 已委派 · 可委派：eslint

- **长这样**：`const data = (res as any).items`、`user!.name`、`// @ts-expect-error 先这样`
- **会怎样**：类型层被绕过；错误推迟到运行时，而且没人知道有多少处
- **期望**：交给 eslint（typescript-eslint）拦；本体不做

**R-11 `console` / `debugger` 残留** · 已委派 · 可委派：eslint

- **长这样**：`console.log('crews', data)`、`debugger`
- **会怎样**：调试残留进生产；日志把用户数据打到控制台
- **期望**：交给 eslint（oxlint 亦内置）；本体不做

**R-12 `TODO` / `FIXME` / `HACK` 残留** · 已委派 · 可委派：eslint

- **长这样**：`// TODO: 这里以后要加权限判断`
- **会怎样**：越积越多，没有人能回答"还有多少没做"
- **期望**：交给 eslint；本体不做

**R-13 吞异常** · 已委派 · 可委派：eslint

- **长这样**：`try { load() } catch {}`、`promise.catch(() => {})`
- **会怎样**：出错静默，排查时完全没有线索
- **期望**：交给 eslint；本体不做

## 七、后补的需求（按疼度排序）

> **开着的：没有了**（`进行中` 0）—— 全部条目都已定：`已完成` 99 · `已委派` 4 · `不做` 7（R-04 / R-49 / R-50 / R-51 / R-52 / R-53 / R-109）。
> **已撤**：R-30 复杂度 · R-70 抑制注释 · **R-94（analytics 面 kit 化）** —— 编号登记在第八节，不复用（见 N-06 / N-07 / N-13）。
> 本节是**后补的一批**（含已完成与判不做的），按疼度排序；更早的按主题分散在一~六节。

**R-73 指纹表要能被项目覆盖** · 已完成 · 本体（要项目声明）

- **长这样**：企业规范只认公司内部那套查询库，指纹表却是本体自带的；或者内置的 `@org/utils` 里有 `formatDate`，
  与成熟的库 API 同名，被"疑似自造轮子"提示了却没法说明"这是我们认可的"
- **会怎样**：要么为了迎合默认指纹改写法，要么整条 P06 / P07 关掉（等于失去这层覆盖）
- **期望**：项目能覆盖单个能力的指纹（加 / 删 pattern、改首选方案），覆盖后的表仍要过校验；
  覆盖不了的能力就明说"这条没法配"，别留个看起来能配的口子。规格见 `.scratch/wheel-detection/`

**R-44 上帝域 / 上帝模块** · 已完成 · 本体（要域划分与项目声明的阈值）

- **长这样**：`crews`、`orders`、`users` 三个域都在 `import { CrewCard } from '@/modules/crews/components/CrewCard'`；或者 `shared/lib/format.ts` 被 40 处引用
- **会怎样**：改一个签名动全仓；这个域想拆也不敢拆了。**文件级**的出入度上限已经有了（单个文件被几十处引用会报），
  缺的是**域粒度**：一个域被多少域依赖、又拖住多少域
- **期望**：项目声明耦合上限后，超过就报；没声明阈值不判

**R-45 状态纪律不统一** · 已完成 · 本体（命名 + 落点，声明才判）

- **长这样**：一半用状态库：`stores/crews.ts` 导出 `useCrewsStore`；一半在页面里 `useState + Context` 自己管
- **会怎样**：新人不知道跟哪套；同一份状态两种心智模型，改起来互相踩
- **期望**：客户端状态的形态与落点在项目里统一，另起一套报。**先要定"什么算一套 store"**

**R-46 权限判断散落** · 已完成 · 本体（复用 callSites / S38）

- **长这样**：`role === 'admin'`、`can('edit')`、`user.permissions.includes('x')` 散在 12 个组件里
- **会怎样**：权限模型改一次要全仓找；漏改的那一处就是越权入口
- **期望**：权限判断只许出现在一处，页面里只消费结论。**先要定"哪些算合法的展示判断"**

**R-47 路由守卫重复写** · 已完成 · 本体（登录路径 + 落点，声明才判）

- **长这样**：三个页面各写一遍 `useEffect(() => { if (!token) navigate('/login') }, [])`，有的用 `Navigate` 组件、有的用 `useEffect`
- **会怎样**：登录态逻辑分散；改一次要改三处，写法还不一致
- **期望**：守卫逻辑收敛到一处，重复写报。**先要定"怎么算重复"**

**R-48 迁移边界只出不进** · 已完成 · 本体（要项目声明 + 层号）

- **长这样**：`src/legacy/**` 是待迁走的旧代码，但新写的 `modules/orders/lib/price.ts` 里 `import { oldPrice } from '@/legacy/pricing'`
- **会怎样**：legacy 从"待清理"变成"事实上的核心"，迁移永远做不完
- **期望**：项目声明哪些目录在迁移中，新代码依赖它们就报（只许反向）

**R-49 公开面破坏性变更（库）** · 不做（用户决定） · 可委派：外部工具（api-extractor 类）

- **长这样**：`@org/ui` 发 v1.2 时删掉了 `export type CrewDto`，下游 `import { CrewDto } from '@org/ui'` 全部编译崩
- **会怎样**：下游被静默破坏；门禁说"结构、命名、宽度都对"
- **期望**：**不做** —— 本期只做前端应用，库的公开面快照排在范围外（要做也得先划清与「不提供存量豁免」的界限）。

**R-50 monorepo 跨包** · 不做（用户决定） · 本体（跨包结论要统一退出码）

- **长这样**：`packages/app/src/x.ts` 里 `import { internal } from '@org/ui/src/internal/secret'`（绕过包的公开面）；或者 `ui` 包反过来 import `app`；跑门禁要一个个包手动跑、没有统一结论
- **会怎样**：包与包的边界形同虚设；CI 里靠脚本拼退出码，漏一个包没人知道
- **期望**：**不做** —— monorepo 跨包是另一个量级的工程（逐包跑 + 汇总结论 + 统一退出码），暂不推进；规格停在 `.scratch/pnpm-workspace/` 备用。

**R-51 委派出去的检查没人验证** · 不做（用户决定） · 本体（只有本体知道委派了什么）

- **长这样**：文档里写着"`any` / console / 复杂度委派给 eslint"，但 `devDependencies` 里没有 typescript-eslint，`lint` 脚本里也没跑 eslint
- **会怎样**：上面那 7 条 `已委派` 的需求**零覆盖**，团队却以为有人管
- **期望**：**不做** —— 委派出去的那些检查由宿主自负（我们不再提供"跑了没跑"的判定）。
  这层风险在评审时已经说清：委派出去等于把覆盖责任交给宿主（见 `docs/DELEGATION-REVIEW.md`）。

**R-52 TypeScript 严格度** · 不做（用户决定） · 本体（编译器选项，生态不管）

- **长这样**：`tsconfig.json` 里 `strict: false`（或者没开 `noUncheckedIndexedAccess`），门禁照样全绿
- **会怎样**：类型层最基础的安全网被关掉了，但没人知道
- **期望**：**不做** —— `tsconfig` 的严格度开关交给项目自己管（本仓 `strict` / `noUnusedLocals` 都开着，但那是项目选择，不是门禁要钉的红线）。

**R-53 聚焦测试残留** · 不做（用户决定） · 可委派：eslint

- **长这样**：`useCrews.test.ts` 里有人调试图方便写了 `it.only(...)`
- **会怎样**：其余测试**静默不跑**，门禁照样绿
- **期望**：**不做** —— 交给宿主的 eslint 测试插件（`no-focused-tests` 之类），本工具不重复实现。

**R-74 规则"在某范式下永远不命中"没人发现** · 已完成 · 本体（元门禁）

- **长这样**：这次发现 6 条已实现的规则因为两行数据缺失（`canonical()` 的角色没声明 `group: 'domain'`、
  `routes.tsx` 没标 `entry: true`）**在前端应用里从来没跑过** —— 门禁一直显示"通过"，实际那 6 条是空的
- **会怎样**：声明合法、规则存在、夹具也有（走手写 roles 的那条路），但**范式预设这条路是断的**；
  只有拿 canonical 真跑一遍才暴露（我们是手工核对撞见的）
- **期望**：断言**范式预设的结构词汇完整** —— 每个范式预设必须声明 `group`（有域/切片捕获时）与 `entry`
  （语义上有入口时），由 `tests/paradigm-coverage.test.mjs` 钉住；只适用某范式的规则显式登记

**R-75 写错的声明会安静地什么都不判** · 已完成 · 本体

- **长这样**：`structure.isolate: ['slices']`（少一个字母）或 `publicApiUnits: [{ role: 'nope' }]` ——
  配置解析得过，运行时一条都不命中
- **会怎样**：项目以为"配了这条纪律"，实际是零覆盖；与"规则没跑要明说"（R-25）同类，但发生在**声明**这一侧
- **期望**：所有 `structure.*` 声明在解析时校验"可命中性"：维度名必须是真实捕获名 / 角色 id 必须真实存在 /
  glob 非空 / **数字字段必须是整数**；写错就报错并列出可用值
  （最后一类是搭 `examples/full` 时撞出来的：`pluralConsistency.layers` 是**层号数组**，
  写成层名 `['modules']` 不报错、S31 静默空转 —— 比较永远为假，规则"在跑"却什么都不判）

**R-76 组件库调用里的文案没判** · 已完成 · 本体（C01 扩展）

- **长这样**：`message.success('保存')`、`notification.open({ message: '保存' })` ——
  JSX 那一半判了，但**组件库调用里的文案**漏着
- **会怎样**：同一个痛点（这句永远翻译不了），只是换了写法；只判 JSX 会让人以为"文案都管住了"
- **期望**：`copy({ messageApis: [...] })` 声明"哪些调用的字符串实参算文案"后，落点外的裸文案报（warn）

**R-77 声明配了却 0 命中** · 已完成 · 本体（报告自述）

- **长这样**：`structure.migrating: ['src/legacy/**']` 写得没错，可项目里那个目录早就没了；
  `clientState.in`、`authRedirects.in`、某个组维度同理
- **会怎样**：门禁显示"通过"，而那条纪律**这次什么都没看** —— 与"规则没跑要明说"（R-25）同类，
  只是发生在声明的**运行期**这一侧（配置期的可命中性由 R-75 管，管不到"角色表里有这个维度、
  但没有任何文件命中它"）
- **期望**：报告里自述"有 N 条声明 0 命中"并点名是哪几条（方案面那一侧见 R-86）

**R-78 埋点事件名散写** · 已完成 · 本体

- **长这样**：`track('crews_view')` 的事件名在各处手拼；改名漏一处 = 数据断层，而分析平台不会报错
- **会怎样**：与缓存键（D22）、路由路径（D23）是同一族问题——**字面量没有唯一出处**
- **期望**：事件名只许出现在声明的唯一出处；调用点直接传字面量就报

**R-79 环境变量 / 开关读取散落** · 已完成 · 本体

- **长这样**：`import.meta.env.VITE_API_BASE` 在十几个文件里直接读；改名 / 换环境全仓搜
- **会怎样**：配置契约散落，且**读的是原始字符串**（没有默认值、没有校验、没有类型）
- **期望**：这些读取只许出现在声明的配置模块；别处报

**R-80 色板与语义令牌的分层** · 已完成 · 本体

- **长这样**：组件样式里 `color: var(--sh-static-gray-3)` 直接引用底层静态令牌
- **会怎样**：换主题时这一处不跟着变（语义令牌层被绕过），且色板里混进语义值
- **期望**：D02（色板只放静态值）+ D18（组件样式只消费语义令牌）——这两条**设计里早就写了、至今未实现**

**R-81 生成物被手改** · 已完成 · 本体

- **长这样**：生成的类型 / 客户端文件没有 `@generated` 标记，有人顺手改了它，下次重生成被覆盖
- **会怎样**：手写逻辑静默消失（或生成物与源不一致）
- **期望**：带标记的生成文件被修改就报

**R-82 组件库调用里的对象实参文案** · 已完成 · 本体（C01 收尾）

- **长这样**：`notification.open({ message: '保存' })` —— 字符串在**对象实参**里，`facts.calls.stringArg` 看不见
- **会怎样**：C01 已判 JSX 与直接传参两种形态，唯独这种漏着；同一痛点换个写法就绕过去了
- **期望**：对象字面量里的文案属性也判（需要事实模型补"对象属性 + 所属调用"）

**R-83 内联样式绕过令牌** · 已完成 · 本体（D15，0.4.0 收回）

- **长这样**：`<div style={{ color: '#ff5a1f', margin: 13, zIndex: 9999, transitionDuration: '200ms' }}>`
- **会怎样**：同一条红线，写进 `.module.css` 会报（D01 / D12 / D13 / D14），写进 `style={{}}` **一条都不报** ——
  换个写法就绕过整套令牌；换主题时这些值不跟着变，而 review 里一句话看不出来（实测过：D 域零命中）
- **期望**：内联 `style` 与 `CSS` 走**同一套刻度** —— 颜色属性里出现颜色字面量就报；长度 / 层级 / 时长三族按
  `valueWhitelists` 声明判（哪族声明判哪族），`var(--token)` 与白名单里的值不报

**R-84 请求策略 / 业务阈值数字散落** · 已完成 · 本体（D20，0.4.0 收回）

- **长这样**：`staleTime: 300_000` 写在三个 hooks 里、`PAGE_SIZE = 20` 写在两个页面里；要统一改成 5 分钟 / 50 条，全仓搜
- **会怎样**：策略与阈值是**跨模块的口径**，散着写就没人说得清"现在到底是多少"；改一处漏一处 = 行为不一致
- **期望**：项目声明"这些名字的数字必须有家"（`designSystem({ numberHomes: [{ name, names, in }] })`）：
  声明之外的文件里给这些名字写数字字面量就报；落在声明的家（如 `src/shared/config/constants.ts`）里不报；不声明不判

**R-85 同一数值跨文件重复出现** · 已完成 · 本体（D19，0.4.0 收回）

- **长这样**：`padding: 12px` 在 7 个组件的样式里各写一遍、`z-index: 9999` 在 3 个文件里重复；项目还没声明刻度
- **会怎样**：这其实是"**事实上已经定下来的口径**"，只是没有名字 —— 下次谁改都不确定要动几处，新人照抄最近的那份
- **期望**：同一 `(属性, 数值)` 在 **≥3 个非令牌文件**里重复出现就提示（warn）"该提取令牌"；
  令牌 / 色板文件里的值不算（那里本来就是它的家）；颜色唯一性归 D03，不重复报；已在声明刻度里的值不提示

**R-95 "来源"这类取值只能靠背字符串** · 已完成 · 本体（导出面）

- **长这样**：`callSites([{ name: '本地存储', from: 'platform.storage', … }])` —— 用户不知道有哪些取值，
  编辑器给不了补全，拼错只能等报告事后发现（`from` 写错 = 那一组安静地不判）
- **会怎样**：新词汇没有"名字"，用户只能翻文档；重构也带不动（字符串不会跟着改名）
- **期望**：数据表的值以**常量命名空间**导出（`callSiteSources.platform.storage`），
  且**从 id 表派生**（单一出处，不许出现第二份清单）；未知值照旧 fail-closed
  （`from` 写错在**配置期**报错并列出可用值）

**R-110 多租户上下文与权限点的唯一出处** · 已完成 · 本体（`callSites.args` + `permissions()` 面 + D29）

- **长这样（三个都会疼的形状）**：
  ① 租户 id 从 URL query / props / 全局变量随手读：`const tenantId = searchParams.get('tenant') ?? user.tenant`；
  ② 权限点写成字面量散落：`can('crew:edit')` / `role === 'admin'` 在 12 个组件里各写一遍；
  ③ 列表接口不带租户维度：`fetch(`${API}${ENDPOINTS.crews}`)` —— 后端按**默认租户**返回，界面上看不出错
- **会怎样**：① 是**跨租户串数据**（企业级最危险的一类事故，且往往只在某个入口漏了）；② 加一个权限点 /
  改一次命名要全仓找，漏一处就是越权或功能凭空消失；③ 数据"看起来对"，直到有人换租户才发现串了
- **现在的状态**：**权限判断的落点**已有（R-46，复用 S38 `callSites`：判断只许出现在声明的守卫文件里）；
  但**租户上下文的来源**、**权限点名的唯一出处**、**请求是否带租户维度**三件事**一条判据都没有**
- **期望**：租户上下文有**唯一来源**（声明的 provider/落点，别处取用即报，与 S44「环境读取落点」同族）；
  权限点名有**唯一出处**（常量表，`can('字面量')` 即报 —— 与 D24「事件名唯一出处」同族）；
  请求经**带租户维度的封装**（由声明的数据层落点保证）
- **判不做的两半**（判据不稳）：① `user.role === 'admin'` 这类**比较**形态 —— 事实模型看不见
  （`reads` 只收 `ENV_READ_ROOTS` 的链），且"展示判断"与"决策判断"不可区分（R-46 非目标）；
  ② "请求有没有带租户维度" —— 租户可能在 header / cookie / token 里、或由服务端按 token 定，
  前端事实看不见，判了会把正确项目刷红（同 N-10 / N-11 的处理）

**R-111 错误与重试策略的唯一出处** · 已完成 · 本体（`errorPolicy()` 面 + D30；错误码映射那半留待）

- **长这样**：`retry: 3` / `retry: 0` / 自己写 `setTimeout` 重试散在各 hook；`retryDelay` 每个域一个算法；
  错误码到用户文案的映射散在组件里（`if (err.code === 'RATE_LIMIT') setMsg('太频繁了')`）；
  全局错误呈现（error boundary / onError）没有落点，谁都没接
- **会怎样**：一次后端抖动变成重试风暴（每个页面各自重试 3 次 × N 个并发查询）；同一个错误三个入口
  三种说法，客服拿着日志对不上；未捕获的渲染错误直接白屏
- **现在的状态**：策略里的**数字**已经有家（D20 `numberHomes`：`staleTime` / `retry` 只许出现在声明的策略文件里）；
  但**重试的形态**（函数式退避 / 条件重试）、**错误码 → 文案的映射**、**全局错误的呈现落点**都没有判据
- **期望**：失败处理的**策略**只许来自声明的落点（形态 + 数字，D20 那条的推广）；错误码到文案的映射有
  唯一出处（与 D22–D26「唯一出处」族同族）；**不该判的**：`catch` 里该做什么（重试/降级/上报）是业务语义
- **这一轮落了哪半**：**策略形态**（D30 + `errorPolicy()`）：函数型 / 枚举型策略只在声明的落点，名字由
  kit 给；数字型仍归 D20（同一处不会两条都报）。**错误码 → 文案映射**（原计划 D31）**留待**：
  它需要新事实（比较表达式），且同 N-12 的处理方式 —— 先要真实项目形态再落
- **被真实误报逼出来的边界**：只判"策略对象**作为调用实参**"。`retry` 这个名字在示例里撞了两处
  （`queryState.ts` 的 UI 回调、i18n 的 `retry: '重试'`），不收窄就会误报 —— 与 N-10 记的"撞名"同因

**R-112 E2E / 契约测试的落点** · 已完成 · 本体（`tests.homes` + M10 + M08/M09 裂缝修复）

- **长这样**：e2e 与单测混在同一个 glob（`src/**/*.test.ts`，或者干脆把 e2e 写成 `*.test.tsx`）；
  e2e 直接 `import { toCrewRow } from '@/modules/crews/lib/mapper'`（测的是实现细节，重构就红）；
  契约测试（拿生成的类型 / 后端 schema 对账）没有落点，于是没人写；`checkChain` 只跑单测，e2e 从未在 CI 执行
- **会怎样**：CI 分不清"快反馈"与"慢验证"，要么慢到没人跑，要么把慢测试塞进单测把门禁拖垮；
  e2e 断言实现细节 → 每次重构都要改测试（最后没人敢重构）；契约漂移无人发现（前后端一起绿、上线才炸）
- **现在的状态**：`test` 角色 + M08 `requireTestsFor` + M09 `checkChain` 只覆盖"**单测**"这一层；
  e2e / 契约测试**连契约域都进不了**（`e2e/**` 不在 `src/**` 里），所以既没有落点也没有判据
- **期望**：测试**分层有声明**（每层一个角色/落点：单测 · e2e · 契约）；e2e **只许经公开面/应用入口**
  （复用 S23/S04/S05 的机制：测实现细节即报）；`checkChain` 能表达"CI 里真的跑了哪几层"（M09 扩展）；
  **不该判的**：测多少、覆盖率够不够（语义判断）
- **这一轮落了什么**：`metrics({ tests: { homes } })` + **M10**（三条判据）+ **顺手修了 M08/M09 的三个既有裂缝**：
  ① M08 的同名配对用**全局 basename** —— `modules/<域>/lib/mapper.ts` 在多个域里同名，"删掉其中一个域的测试照样绿"；
  现在必须有**同目录**配对（或顶层 `tests/` 下的同名文件）。② M08 的测试文件判定只认 glob，
  而 `canonical()` 给了 `**/__tests__/**` 测试**角色** —— Jest 形态的项目会假红；现在**角色与 glob 都认**。
  ③ M09 用 `chain.includes(cmd)` 子串匹配 —— `self-test` 满足了"跑过 test"、"test:e2e 从没跑"却一声不吭；
  现在按**脚本名解析 + 一跳闭包**（`check → test → …`），认不出的直接命令放过。
- **两套示例**：canonical 加了 `e2e/`（只经应用入口）与 `tests/contract/`（引用生成的契约，且**真的在 `node --test` 里跑**），
  配置里 `include` 覆盖测试层 · 每层一个角色 · `entries` 补应用入口 —— 三条配套缺一条就会空转

**R-113 配置放错层会静默失效** · 已完成 · 本体（配置期键白名单）

- **长这样（我自己在 B1 里踩的）**：把 `addRoles` 写进 `overrides.structure` 而不是 `overrides` ——
  键名对、值是好的，只是**放错了一层**；引擎不认识的键被静默忽略，S01 照旧报"文件不在目录契约内"，
  人只会去怀疑自己的 glob 写错了
- **会怎样**：声明型配置最怕的就是这个 —— 写了、看着对、实际没生效，排查时间全花在错的地方
  （与"声明 0 命中"同族，但那一条只覆盖**方案面**的参数）
- **期望**：配置里不认识的键（`structure` / `overrides` / 面参数）在**配置期报错**并列出可用键 ——
  fail-closed，而不是静默忽略；边界：`params` 里范式/kit 自带的键不在此列（它们由适配器校验）

**R-109 本地 import 的路径解析不到，没有任何人报** · 不做 · 本体（委派给 eslint `import/no-unresolved`）

- **长这样**：`import { x } from '@/entities/crew/nope'` —— 文件根本不存在（改名、打错、删错了目录）
- **实测**（2026-09-25 探针，`examples/full-fsd`）：把 `entities/crew/@x/order.ts` 改名但不同步导入路径 →
  整个守卫 **0 finding**。原因：S45 判的是"**路径解析得到、名字对不上**"（解析不到时按边界跳过，分不清
  第三方包与写错的本地路径）；依赖图规则全在**边**上工作，没有边就没有判定
- **结论：不做，交 eslint `import/no-unresolved`** —— 这条**早就判过**：本体原先自己实现（**S33**），
  0.4.0 主动移除（`docs/DESIGN.md` §4.9 · `docs/DELEGATION-REVIEW.md` · `docs/ALTERNATIVES.md`：
  "路径写错是编辑器/编译器就该说的错，不该占目录契约的一条红线；射程与 tsc 重合，增量价值≈0"）。
  本轮先按"新需求"开了 S47 的原型，翻到这条记录后**全部撤回**（规则 / 夹具 / 引擎改动都没留）
- **委派是否兑现**（我们自己的仓库）：`src/**` 有 **tsc** 兜（实测 TS2307，且 `pnpm check` 跑 tsc）✓；
  但 `eslint.config.mjs` 里**没有任何 `import/*` 规则**、devDeps 里也没有 resolver ✗ ——
  所以**示例目录**（不跑 tsc、只跑守卫与自带测试）里这类错仍然静默。要补上需要 2 个 devDep
  （`eslint-plugin-import-x` + `eslint-import-resolver-typescript`），而它带的 `unrs-resolver`
  有 postinstall —— 在 `pnpm-workspace.yaml` 的 `allowBuilds` 里放行/显式拒绝是**宿主的供应链决定**，
  本轮没有替宿主做，留作待定

**R-108 示例自带的测试没有门禁跑** · 已完成 · 本体（示例链路）

- **长这样**：示例里有 `pnpm test`，但 `pnpm check` 只跑守卫 —— 示例唯一的测试坏了两周没人知道
  （实测 `0 pass / 1 fail`：它 import 的常量在目标文件里根本不存在）
- **会怎样**："活样板"悄悄烂掉，照抄的人拿到的是坏的；而门禁一路绿
- **期望**：示例自带的测试进 `pnpm check`（`examples:test`），坏测试会让整仓门禁变红

**R-107 import 的名字在目标文件里根本不存在** · 已完成 · 本体（S45 新规则）

- **长这样**：`import { crewPolicy } from '@/shared/api/client'`，而 `client.ts` 没有这个导出
  （本仓示例真发生过 4 处；另有一处测试 import 了不存在的常量）
- **会怎样**：TS 编译不过 / 运行时 `undefined`，而门禁 **0 finding 显示通过** —— 最容易发生的一类假绿
- **期望**：本地 import 的具名成员必须真的被导出（`default` / 星号导出 / CSS 等非 TS 目标豁免）；
  前置：事实模型记录**导入的具名成员**（`ImportFact` 现在只有 `spec`）

**R-106 用户不知道下一步该配什么** · 已完成 · 本体（能力配方 + `init`）

- **长这样**：`pnpm check` 只说"因能力未声明而停用 29 条"，不说"想让它跑要写哪一行"；
  文档教的是"配全"（139 行）；新词汇（`from` / `numberNames` / `singletons`）只能翻文档
- **会怎样**：新项目卡在"不知道补什么"，要么不接、要么照抄一大坨；配置复杂度直接劝退
- **期望**：① 停用清单给出"补什么 + 可复制片段" ② 违反时报告附可复制的配置 ③ `arch-guard init`
  按选择生成填好落点的配置 ④ USAGE 改成"5 行起步 → 按报告补 → 最终形态"

**R-105 官方 `@x` 跨引用公开面未实现** · 已完成 · 本体（角色表 + S22/S23 例外）

- **长这样**：`entities/artist/@x/song.ts`（官方给实体互引的唯一出口）现在报 S01"文件不在目录契约内"
- **会怎样**：实体交叉引用时只剩复制类型/上提/违规三条路，前两条都在啃层语义
- **期望**：认得 `@x/<切片>` 为公开面，且**只放行被指名的那一侧**（S22 开这一条例外）；夹具演示

**R-104 复数文案写不出来** · 已完成 · 本体（C02/C03/C06 复数口径）

- **长这样**：`count: '{{count}} orders'` —— 英文恒 "1 orders"；想写 `count_one` / `count_other`
  会撞 C03"两语言键必须全等"
- **会怎样**：复数这条照抄就错，英文界面第一屏就露
- **期望**：C03 对 i18next 复数后缀（`_one` / `_other` …）豁免，改为两语言的**复数集合**对账；
  示例演示（A8）

**R-103 声明了样式/令牌目录却没有任何入口** · 已完成 · 本体（D28）（新规则）

- **长这样**：`palette` / `theme` / `base` / `vendor` 四份全局 CSS 没有任何 import，`index.html` 也没有 link
- **会怎样**：**门禁绿但主题静默不生效** —— 照抄的人以为配好了
- **期望**：声明了 `styleDir` / `tokenDir` / `vendorDir` 就必须有文件真的被引用（或被 `index.html` 引）；
  示例补样式入口（A6）

**R-102 对比度只声明了必然通过的那一对** · 已完成 · 本体（D27）（新规则）

- **长这样**：组件里 `color: var(--surface)` + `background: var(--brand)`（语义令牌当反色用），
  而 `contrastPairs` 只声明 `--text-primary` / `--surface`
- **会怎样**：浅色下白字橙底 ≈3.1:1（**低于项目自己声明的 4.5**），dark 反成 5:1 —— 达标随主题反转，
  而门禁抓不到（它只查声明的那一对）
- **期望**：组件样式里出现的 `(前景, 背景)` 令牌组合必须在 `contrastPairs` 里声明（或改用声明的 `--on-*` 角色）；
  示例补 `--on-brand` 并演示（A7）

**R-101 埋点在 render 体内调用** · 已完成 · 本体（S46）（新规则）

- **长这样**：`CrewsPage` 组件体里直接 `sendEvent(crewsViewEvent)`
- **会怎样**：每次重渲染都上报（含 StrictMode 双调用）→ 埋点虚高、计费失真；
  D24 只看事件名是不是字面量，**看不见调用位置**
- **期望**：页面/组件的**渲染体**里不许直接调声明的埋点上报 API（只许在 effect / 事件回调里）；
  示例改成 `useTrackView(event)`（A5）

**R-100 缓存键形状不一致** · 已完成 · 本体（D26）

- **长这样**：`list: ['crews']` 与 `detail: (id) => ['crews', 'detail', id]` 混用，或写成 `['crew', id]` 单数
- **会怎样**：`invalidateQueries({ queryKey: ['crews'] })` 前缀不匹配 → **缓存不失效、数据不刷新**；
  D22 只查"字面量在不在家"，形状漂移一个都不报
- **期望**：同一个 key 工厂的键数组必须以**同一前缀**开头（可判定）；前置：数组首元素要带上
  所属导出 + 属性路径（现在箭头函数体里的 `prop` 是 null）

**R-99 后端端点没有唯一出处** · 已完成 · 本体（D25 + `endpoints()` 面）

- **长这样**：``fetch(`${API_BASE_URL}/crews?limit=${n}`)`` 写在 API 客户端里，另一个文件再拼一次同一个端点
- **会怎样**：改接口漏一处 = 404 / 数据断层 —— 与缓存键 / 路由 / 事件名同族，唯独端点没人管
- **期望**：新增受体（`endpoints({ from })`）+ 规则：端点字面量只许出现在声明的出处；
  **前置**：事实模型要能看见模板串的静态段（现在端点写成模板串后在事实里**是隐形的**）

**R-98 域没有业务公开面，跨域只能抬进 shared** · 已完成 · 本体（角色表 + 公开面判定）

- **长这样**：canonical 的域公开面只有 `routes.tsx`（路由入口）；跨域想共享实体时没有合法通道 ——
  `isolate: ['domain']` 禁直连、S23 禁绕过入口
- **会怎样**：唯一出路是"抬进 shared"，于是 shared 长出"第二套 modules"（DTO / 键 / 策略 / 组件全抬上去），
  域边界变成装饰（两份独立审查都指向这里）
- **期望**：canonical 声明域级业务公开面（`modules/<域>/index.ts` 作为 entry）；S23 允许"跨域只经它"；
  示例真演示一次跨域协作（A4）

**R-97 `queryKeyFrom` 只能一个文件** · 已完成 · 本体（能力扩展）

- **长这样**：键表按域拆成 `modules/<域>/model/keys.ts` 时，`dataLayer({ queryKeyFrom })` 只收一个路径 →
  其余键文件里的字面量全被判"出现在落点外"
- **会怎样**：键只能集中在一个 shared 文件（域一多就是横向 merge 队列），或者干脆不声明、D22 整条停用
- **期望**：`queryKeyFrom` 收数组 / glob；D22 判"字面量出现在**所有**落点之外"；
  R-86 的"0 命中"自述按落点逐个点名；示例把键拆到域 / 实体（A3）

**R-96 `fsd()` 的样式落点不合规范，布局没地方放** · 已完成 · 本体（预设默认 + 示例）

- **长这样**：用 `fsd()` 的项目，设计令牌被预设放进 `shared/ui/styles/tokens`（`ui` 是官方段名，令牌"顺手"挂它下面）；
  而页面布局没有规范位置，只能塞进 `app/routes/` 里
- **会怎样**：① 官方对 shared 段的要求是"**按用途命名**"（`components` / `hooks` / `types` 是它点名的坏名字），
  `ui` 段里塞样式令牌是同一类问题；而且它作为 `shared/ui` 的子目录还得有一个**只为占位**的 `index.ts`
  ② 官方 app 典型段（routes / store / styles / entrypoint）里没有"布局"，而官方在 Widgets 一节明确写
  "页面布局可以放在这一层"—— 布局挤在 routes 里，读者会以为它是路由配置
- **期望**：`fsd()` 的 `styleDir` / `tokenDir` / `vendorDir` / `paletteFile` / `themeFile` 都落在官方 app 段的
  `src/app/styles` 下；示例把布局移进 `widgets/app-layout/`（顺带用上 widgets 层）；并且**落点参数写错要自述** ——
  `designSystem.tokenDir` / `paletteFile` … 指向不存在的路径时那几条规则**安静地返回空**
  （与 R-86 同族"声明配了却 0 命中"，这次一并补上）

**R-90 策略数字的名字每个宿主抄一遍** · 已完成 · 本体（适配器数据）

- **长这样**：`designSystem({ numberHomes: [{ name: '请求策略', names: ['staleTime', 'retry'], in: [...] }] })`
  —— `staleTime` / `retry` / `gcTime` 是 **react-query 自己的 API 名**，却要每个宿主抄一遍；
  抄错一个字母那一组就静默少判（R-75 那一族）
- **会怎样**：换数据层方案要连名字一起改；新项目不知道该写哪些名字，于是 D20 那一半不生效
- **期望**：库的事实放适配器（`reactQueryKit().numberNames`），宿主只写"**家在哪**"（`in`）；
  项目自己的常量（`PAGE_SIZE`）仍然显式列；`names` 缺省且适配器也没给 → 这一组不判（不是"全都报"）

**R-91 调用落点要把"这类调用叫什么"抄一遍** · 已完成 · 本体（适配器数据 + 数据表）

- **长这样**：`callSites([{ name: '本地存储', apis: ['localStorage'], in: [...] }])` ——
  `localStorage` 是浏览器平台的、`QueryClient` 是 react-query 的、`gtag` 是分析 SDK 的，
  全是**别处已有的知识**；写错来源 = 那一组安静地什么都不判
- **会怎样**：新项目不知道要写哪些名字，于是 S38 那一层不生效；换个 SDK 要翻所有配置
- **期望**：`from` 指向一处既有清单 —— 平台表（`platform.storage` / `timers` / `network`）·
  分析 SDK（`analytics.gtag` / `segment` / `sentry`）· 你选的 kit（`data-layer.singletons`）；
  项目自己的封装仍写 `apis`（`apis` 优先）；来源写错在**配置期**报错并列出可用值；
  来源解析不出名单时由报告自述（不是静默）；取值以**常量**导出，见 R-95

**R-92 "0 命中"自述会把适配器给的默认清单也算成项目声明** · 已完成 · 本体（报告自述）

- **长这样**：`reactQueryKit()` 自带 8 个取数钩子，项目只用其中 1 个 → 自述里出现
  「data-layer.fetchApis 的调用名 useQueries / useMutation … 0 命中」，逼着宿主把清单收窄成
  `fetchApis: ['useQuery']`（把 kit 的事实再抄一遍，且收窄没有任何收益）
- **会怎样**：自述变成噪音，用户学会忽略它 —— 那 R-86 那条真话（项目名字写错）也一起被忽略
- **期望**：自述只盯**项目自己写的**名字 / glob（`analytics.apis` · `callSites[].apis` · 各面落点 ·
  `structure.*` · `numberHomes`）；适配器 / 平台给的既有清单（`fetchApis` · `env-reads.apis`）不点名

**R-93 环境读取的"读取根"每个宿主抄一遍** · 已完成 · 本体（平台表）

- **长这样**：`envReads({ apis: ['import.meta.env', 'process.env'], in: [...] })` ——
  `import.meta.env` 是 Vite 的事实、`process.env` 是 Node 的事实（平台表里本来就有）
- **会怎样**：跟 R-90 / R-91 同一件事：库 / 平台的知识被写成项目配置，写错还静默
- **期望**：`apis` 可以省略（缺省取 `data/env-roots.ts` 的平台表），宿主只说"**只许在哪读**"（`in`）；
  显式给了仍按项目给的判并参与"0 命中"自述

**R-89 名单来自"适配器默认"还是"项目声明"，报告不说** · 已完成 · 本体（报告自述，R-86 同族）

- **长这样**：项目原来用 antd（`uiKit(antdKit())`，C01 的文案位名单来自适配器），后来换成另一个 kit
  （或那个 kit 没声明 `messageApis`）—— 那一半**安静地关掉**，报告显示"✔ 通过"，一个字都不提
- **会怎样**：`requires` 是**整条规则**粒度，管不了"一条规则两半"（C01 的 JSX/属性那半靠 i18n、
  组件库调用那半靠名单）。于是"少判一半"既不在 findings 里，也不在"因能力未声明而停用"里
- **期望**：报告自述名单来源，且四种取值互相不可混淆：`copy({ messageApis }) 项目声明 N 条` /
  `uiKit(antd) 默认 N 条` / `copy({ messageApis: [] }) 显式关掉` / `无 —— 这一半没在判`；
  C01 没在跑时不说（那种情况已由「因能力未声明而停用」明列）

**R-88 换组件库还得把库的 API 名单抄一遍** · 已完成 · 本体（适配器数据，C01 的名单来源）

- **长这样**：项目用了 antd + i18n，`copy({ messageApis: ['message.success', 'notification.open', …],
messageProps: ['message', 'description', 'title'] })` —— 这两张表是 **antd 的事实**（它自己的 API），
  却要每个宿主照抄一遍；不抄，C01 的"组件库调用里的文案"那一半就永远不判，而报告也不提
- **会怎样**：新项目/新同事根本不知道要写这两行 → `message.success('保存')` 一路漏到线上；
  装了库还得配，正是本仓一直在收回的那些"生态插件要你维护第二份清单"
- **期望**：库的事实放**适配器**（`uiKit(antdKit())` 声明 antd 的 `messageApis` / `messageProps`），
  宿主一个字不用抄；**项目自己的封装**（`appToast.success`）仍由 `copy({ messageApis })` 声明，
  且**项目声明覆盖适配器默认**（传 `[]` = 显式关掉这一半）；换库只换适配器，名单跟着走

**R-87 同一批"与范式无关"的规则只在应用范式下启用** · 已完成 · 本体

- **长这样**：项目用 `fsd()`（或 `library()`）—— 配置里老老实实配了 `clientState` / `authRedirects` /
  `couplingLimits` / `maxRelativeUp`，但 **S41 / S42 / S39 / S43 根本没被任何预设启用**
- **会怎样**：既不跑、也不在报告的"停用"清单里（`skipped` 只收"能力未声明"的）—— 宿主以为配了四条纪律，
  实际零覆盖，报告一个字都不提。与 R-74（规则在某个范式下**永远不命中**）同一家族，但更靠前一步：
  **规则压根没注册**。实测：`fsd()` 复用 `library()` 的启用清单，那份清单少了 9 条与范式无关的规则
- **期望**：范式无关的规则在每个范式下都要**注册**（要项目声明的那些没声明就明列停用，不许消失）；
  只有真正应用专属的（S03 · S04 · S05 · S06 · S09 · S14 · S15 · S18 · S19）允许缺席 ——
  这份名单写成显式期望表，用两份真实示例（`canonical` / `fsd`）的注册差集判定，**多一条就红**
  （`tests/paradigm-coverage`）

**R-86 方案面的声明配了却 0 命中** · 已完成 · 本体（报告自述，R-77 的覆盖面补全）

- **长这样**：`callSites([{ name: 'flag', apis: ['isEnableX'], in: ['src/shared/lib/flags.ts'] }])`（多打一个 X，路径也写错）、
  `envReads({ apis: ['import.meta.envX'] })`、`analytics({ apis: ['trackX'] })`、`numberHomes` 里的名字写错
- **会怎样**：报告里写着"生效的适配器：analytics=declared · call-sites=declared · env-reads=declared"，
  **让人以为这几条纪律在跑**；实际一条都不会命中 —— 名字 / 路径写错一个字母 = 零覆盖。
  R-77 已经管了 `structure.*` 那一侧，但**方案面预设的声明**（`callSites` / `envReads` / `analytics` /
  `router.pathSource` / `dataLayer.queryKeyFrom` / `designSystem.numberHomes`）没人管（实测：三处写错的声明，报告一声不吭）
- **期望**：报告自述"有 N 条声明 0 命中"并点名（同一个 `declaration-no-match`）：落点类看文件 / glob 是否存在；
  清单类（`apis`）在**最宽松的匹配**（整名 / 前缀 / 后缀）下都没有任何调用命中才点名（宁少报不误伤）；
  环境读取的 `apis` 是成员访问链，拿 `reads` 判

## 八、不做（写在这里是为了不再重复讨论）

| 编号 | 内容                                                                                                                                                     | 原因                                                                                                                                                                                                                                                                                                                                                                                                        |
| ---- | -------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| N-01 | SSR / 服务端渲染相关的边界                                                                                                                               | 本期不考虑                                                                                                                                                                                                                                                                                                                                                                                                  |
| N-02 | 存量豁免 / 基线机制                                                                                                                                      | 已移除，不复活（见 R-42）                                                                                                                                                                                                                                                                                                                                                                                   |
| N-03 | 口味类判断（抽象该不该合并、组件该不该拆、命名好不好、翻译质量、视觉品味）                                                                               | 那是 review 的事，不做成红线                                                                                                                                                                                                                                                                                                                                                                                |
| N-04 | 构建与体积门禁                                                                                                                                           | 本体不跑构建（零副作用）；包体积交给专门工具                                                                                                                                                                                                                                                                                                                                                                |
| N-05 | `--watch` / `--fix` / 每条规则的文档链接                                                                                                                 | 判据等价即可，不追这些使用体验                                                                                                                                                                                                                                                                                                                                                                              |
| N-06 | 复杂度 / 嵌套深度 / 参数个数（**原 R-30**，2026-09-25 撤）                                                                                               | 结论：交给 eslint（`complexity` / `max-depth` / `max-params`）；本体不实现 —— 这条委派登记的落点是宿主自己的 lint 配置，不是我们的委派清单                                                                                                                                                                                                                                                                  |
| N-07 | 抑制注释（`eslint-disable` / `@ts-ignore`）报出（**原 R-70**，2026-09-25 撤）                                                                            | 结论：不做。`@ts-ignore` 那一半由 TS 的 `--noUnusedLocals` 一类开关与 review 兜；专门做一条"禁止 suppression"会跟"规则级例外要写理由"重复表达同一个意思                                                                                                                                                                                                                                                     |
| N-08 | 裸 `Math.random()` 这类随机数（**原委派清单里的一项**，2026-09-25 判不做）                                                                               | 结论：不做。产生随机数不一定是错（抖动 / 唯一 ID / 抽样 / 灰度），要判"随机数当数据用"就得猜语义（L5）；真正会疼的那半（命名带 `mock` / `fake` 的数据留在生产路径）已由 H09 覆盖                                                                                                                                                                                                                            |
| N-09 | 可访问性（a11y）形态检查：`<div onClick>` 可点不可聚焦、缺 `alt` / `aria-label`、正数 `tabIndex`、`role` 乱用（**挖掘需求时的一项，2026-09-25 判不做**） | 结论：不做 —— **交给宿主自装 `eslint-plugin-jsx-a11y`**（单文件语法形态 L2、零项目专有数据，与 H01–H05 同类；生态有开箱即用等价物，重写只会重复）。本体只碰可判定的那一半：**D07 对比度基线**（令牌对）与 **C01**（`alt` / `aria-label` 的**文案**必须走 `t()`）                                                                                                                                            |
| N-10 | feature flag 名的唯一出处（`isEnabled('new-nav')` 散写，改名漏一处 = 那个路径永远 false）（**候选，2026-09-25 判不做**）                                 | 结论：不做 —— **判据不稳**：哪些调用算"读 flag"完全靠宿主声明，而 `isEnabled` / `get` / `variation` 这类名字与**权限判断、存储读取、其他 SDK** 撞名（同形不同义，声明写错就两头不讨好）；多参数 API 的第一个实参不是名字（同 D24 的已知边界）；属性形态（`<Feature name=…>`）的 `name` 太泛，进默认必然误伤。**落点那一半本来就能用 `callSites`（S38）覆盖**，剩下的价值不配一条红线                        |
| N-11 | `.env` 声明 ⇄ 代码读取对账（声明了没人读 / 读了没声明）（**候选，2026-09-25 判不做**）                                                                   | 结论：不做 —— **判据不稳**：声明侧可能**不在仓库里**（`.env.local` 被 gitignore、变量由部署平台/CI 注入），读取侧可能在**契约域外**（`vite.config.ts`、构建脚本），两个方向都会误报；S44 只管"读取落点"，那才是可判定的那一半                                                                                                                                                                               |
| N-14 | 错误 / 加载态的**形态**（`{data,isLoading}` / `Result` / 抛异常 / 状态机）—— 候选，2026-09-25 判不做                                                     | 结论：不做 —— 形态自由度大，四种写法都合理；判了就是误报（与 N-09 / N-11 / N-12 同类）。示例里会演一个 `isError` 分支（A2），但本体不立规则                                                                                                                                                                                                                                                                 |
| N-15 | DTO ↔ 领域模型的**映射约定** —— 候选，2026-09-25 判不做                                                                                                  | 结论：不做 —— 是建模选择（mapper / 直接用 DTO / zod 解析都合理），不是可判定不变量；**示例里加显式 mapper**（A2）作为推荐形态                                                                                                                                                                                                                                                                               |
| N-16 | 错误被吞掉 / try-catch 形态 —— 候选，2026-09-25 判不做                                                                                                   | 结论：不做 —— 与 N-14 同源：`catch` 里该做什么取决于业务（重试 / 上报 / 降级），判"吞了错误"必然误伤                                                                                                                                                                                                                                                                                                        |
| N-17 | `@x` 之外的跨切片自由互引 —— 候选，2026-09-25 判不做                                                                                                     | 结论：不做 —— 与 `isolate` 直接冲突；真要放开的只有官方 `@x` 这一条（见 R-105）                                                                                                                                                                                                                                                                                                                             |
| N-13 | analytics 面 kit 化（**原 R-94**：`analytics(gtagKit({ eventSource }))`，让厂商 API 名也由 kit 给）（**候选，2026-09-25 判不做**）                       | 结论：**当前不做** —— 前提没满足：D24 取的是**第一个字符串实参**，而 `gtag("event", "crews_view")` 的第一个字符串是 `"event"`（已知误报）。不先补"事件名在第几个实参"，kit 化等于把已知误报变成默认行为。另一个原因：`eventSource` 永远是**项目落点**，kit 化的收益只落在「直接调厂商 API」的项目上 —— 那类项目现在写一行 `analytics({ apis: ['gtag'], eventSource })` 也就够了                             |
| N-12 | TS 里的颜色字面量（按**文件类型**判"TS 里不许写颜色"）（**候选，2026-09-25 判不做**）                                                                    | 结论：不做 —— **判据不稳**：CSS-in-JS 项目里 TS 主题对象**就是**颜色的家（判了就是误伤），纯 CSS 项目里它才是"第二个家"。所以当前颜色只判两处：**CSS 文件**（D01）与 **JSX 内联 `style`**（D15）；TS 里的颜色**不判**（`D01` 代码注释里那句"留给 D16 那一族"已作废，D16 是路径级的、不看字面量）。将来要做，稳定形态是"**声明的颜色出处可含 TS 文件**"（把 D01 的判据泛化），但那要先有真实项目提出这个场景 |

---

## 附：与其他文档的分工

> **文档地图在 [`README.md`](./README.md) 的「文档地图」一节**（唯一来源）。这里只记住一条：
> 本文件回答**做什么、为什么做、做没做、由谁来做**；怎么实现看 `docs/DESIGN.md` 与
> `docs/ARCHITECTURE.md`，单条的实现规格看 `.scratch/<slug>/spec.md`。
