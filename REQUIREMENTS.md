# 需求

> **这份文档是需求的唯一来源。**
> 要做什么、期望行为是什么，先写在这里，再由它推进；不再散落在对话里。
> 这里**只写人话**：不写规则编号、配置字段、目录路径、实现方案 —— 那些在 `docs/DESIGN.md` 与
> `.scratch/<slug>/spec.md`；`README.md` 的 Roadmap 只标进度；`CHANGELOG.md` 只记发生了什么。

## 怎么用

- 一条需求 = **一个场景**（谁在什么时候会疼）+ **预期行为**（什么时候必须报、什么时候必须不报）。
- 新需求先写进这份文档，再开工；做完把状态改成「已做」，并在 CHANGELOG 记一笔。
- **写不出真实场景的需求先不做**（约定见 `docs/agents/scenario-first.md`）。
- 状态只有四种：**已做** · **待做** · **待定** · **不做**。
- 每条都遵守同一条底线：**宁可漏报，不可误伤** —— 正常写法被报红一次，整条门禁就没人信了。

---

## 一、每天都会踩的（放文件、改引用）

**R-01 文件放错地方**

- 痛点：新人把页面放进域内的工具目录、把类型塞进组件目录；目录契约靠 review 口口相传，过几周就没人说得清。
- 预期：文件不在契约里就报，并告诉它**该放哪**（不是只说"你错了"）。
- 状态：已做

**R-02 跨域直接抓别人的内部文件**

- 痛点：A 域直接引用 B 域的内部实现，B 域一改 A 就崩；"域是自治单元"变成口号。
- 预期：跨域只能经对方的公开入口；直接引用内部文件报。
- 状态：已做

**R-03 循环依赖**

- 痛点：两处互相引用，构建顺序/初始化时机出玄学问题，排查半天。
- 预期：依赖图里出现环就报，锚在环里最容易定位的那个文件上。
- 状态：已做

**R-04 导入路径写错**

- 痛点：相对路径少一层 `..`，解析不到但没人发现，直到运行时才炸。
- 预期：解析不到的导入一律报。
- 状态：已做

**R-05 底层反过来依赖上层**

- 痛点：共享层反向引用业务域、业务域引用装配层，改一处动全身。
- 预期：依赖只能单向（层号不升着走），反向引用报。
- 状态：已做

**R-06 死代码 / 死切片**

- 痛点：没人用的文件、没人引用的模块越堆越多，谁都不敢删。
- 预期：从入口不可达的文件报；整个组没有任何外部引用（死切片）报。
- 状态：已做

**R-07 页面访问不到**

- 痛点：页面写好了但没接到路由上；或者域里有页面却没有域入口 —— 上线才发现打不开。
- 预期：有页面却缺域入口报；页面没被本域入口引用报。
- 状态：已做

**R-08 命名与目录体量的规矩**

- 痛点：组名重名、同一层里单复数混着用、一个目录塞几十个子项、片段里出现保留名目录 —— 每次 review 都要争。
- 预期：深度、槽位、保留名、组名重名、重复词、单复数混用、组数量与目录子项上限，超了都报。
- 状态：已做

**R-09 入口文件的叫法**

- 痛点：项目用配置式路由（入口是 `routes.ts`，不是 `routes.tsx`）被误报；换了入口名又没人记得同步契约。
- 预期：项目声明自己的入口叫法，规则跟着走；声明了入口名却没在契约里登记，明确报"契约没跟上"。
- 状态：已做

## 二、换库 / 换方案时

**R-10 路由路径手写**

- 痛点：`'/crews'` 这种字面量散在路由表、链接、跳转里；改一个路径要全仓搜，漏一处就是 404。
- 预期：路径只许出现在项目声明的唯一出处；别处的路径字面量报。
- 状态：已做

**R-11 缓存键手写**

- 痛点：查询键在各处手拼（`['crews', id]`），改名漏一处就是缓存不命中/脏数据，且极难查。
- 预期：键只许出现在项目声明的唯一出处；别处的键字面量报。
- 状态：已做

**R-12 页面里取数 / 域里直连后端**

- 痛点：换数据层要翻遍页面；契约类型散在十几个域；写测试必须 mock 网络。
- 预期：取数只许出现在项目声明的那几处；页面/组件里直接取数、域里直接发请求都报。测试文件不报（那儿的用法是正常的）。
- 状态：已做

**R-13 埋点上报 / 本地存储散在各处**

- 痛点："用户没同意隐私协议就别上报"没地方统一；token 要加密、存储要加迁移时全仓找，漏一处就是半加密；换 SDK 动十几个文件。
- 预期：这几类调用只许出现在项目声明的封装里，别处报。测试文件不报。
- 状态：已做

**R-14 域里自建配置对象**

- 痛点：某个域自己建了一个查询客户端/主题/状态容器，运行时出现两份实例 —— 缓存失效不生效、主题不统一，查一下午。
- 预期：这类配置对象只许在装配层或共享层建；域里自建报。
- 状态：已做

**R-15 同类方案混用**

- 痛点：装了 react-query 又顺手 import 了 swr，两套缓存并存；换库换一半。
- 预期：登记了某个方案之后再引入同类里的另一个库，报。
- 状态：已做

**R-16 声明与实际依赖不一致**

- 痛点：方案表里写着某个库却没装；装了别的组件库却没登记；新依赖悄悄进来没人审。
- 预期：声明了要装就必须装；装了方案表之外的同类库报；开了白名单时未登记的运行时依赖报。
- 状态：已做

**R-17 样式方案换了形态**

- 痛点：换成 Sass 后 `*.module.scss` 被当成"组件目录里的全局样式"误报；用原子类方案的压根没有组件样式文件。
- 预期：组件样式文件的形态跟着项目声明走；确实没有这种文件的方案，规则不判（而不是乱报）。
- 状态：已做

## 三、上生产前

**R-18 首屏把所有页面都打进去**

- 痛点：路由表静态 import 页面，所有页面随主包下载，首屏变大，第一个月没人发现。
- 预期：项目声明"页面要懒加载"后，入口对页面的静态 import 报。
- 状态：已做

**R-19 测试与覆盖率的退步**

- 痛点：覆盖率一路下滑没人拦；改了代码没补测试也过；覆盖率产物是旧的，门禁拿旧数据装绿。
- 预期：覆盖率不许倒退；本次改动的文件必须被覆盖；产物过期报。
- 状态：已做

**R-20 门禁自己漏跑**

- 痛点：覆盖率一直在跑，但统计的是错的进程；或者有人把测试从提交链路里摘了 —— 这类"假绿"只有门禁自己能查。
- 预期：声明的提交链路里必须真的跑测试与覆盖率，缺了就报。
- 状态：已做

## 四、文案 · 设计系统 · 反退化

**R-21 文案键**

- 痛点：加了文案只写一种语言；两种语言的键对不上；删了组件留下一堆死键；资源目录与声明不一致。
- 预期：键缺语言、目录不同构、死键、声明了却没有资源，都报。
- 状态：已做

**R-22 颜色与令牌**

- 痛点：色值到处硬编码；令牌悬空引用；死令牌；明暗两套名字不一致；对比度不达标；第三方类名逸出 vendor 目录。
- 预期：颜色只在色板、令牌引用闭合、无死令牌、明暗同名、对比度基线、vendor 边界与反向封闭，都报。
- 状态：已做

**R-23 退路留在仓库里**

- 痛点：重构后旧实现文件留在目录里，两年后没人敢删，新人抄了旧的那份，两套行为不一致。
- 预期：文件名带"旧实现"整段标记的报；正常模块（名字里恰好含这几个字）与迁移目录不误伤。
- 状态：已做

**R-24 换库没换干净**

- 痛点：换了组件库，旧库的选择器/变量还在样式里；框架语法残留（如 Vue 的 `@apply`）。
- 预期：残留报。
- 状态：已做

## 五、治理：要能解释、能追责

**R-25 规则没跑要明说**

- 痛点：报告显示"通过"，其实是"因为项目没配，这条压根没跑" —— 最危险的静默失效。
- 预期：没跑的规则在报告里**逐条列出停用原因**，并且不计入"通过"的自我感觉里。
- 状态：已做

**R-26 生效的是哪套方案要看得见**

- 痛点：换库换错了、组合配置时手滑多写一份，报告里完全看不出来，只能翻配置文件。
- 预期：每次运行都自述"生效的这几套方案是什么"；要字段级全貌时有一条命令列全表。
- 状态：已做

**R-27 同一个方案被声明两次**

- 痛点：两份配置声明同一个位置，悄悄取后者生效；出问题时无从判断。
- 预期：内容不同的两份声明直接报错（要覆盖就走显式覆盖入口），不静默取后者。
- 状态：已做

**R-28 豁免要有代价**

- 痛点：曾经的"一键重写基线"让"重新写基线"永远比"修"便宜，门禁退化成"没有新增违规"。
- 预期：豁免只能是"某条规则对某类文件不适用"，必须写清理由、可设到期日，过期即红；**不提供**一键重写基线的退路。
- 状态：已做

**R-29 手搓轮子**

- 痛点：自己写了一个 `debounce` / 深拷贝，形态和成熟库一样，但少了取消、边界处理这些坑。
- 预期：形态证据 + 与成熟库 API 同名**两条同时成立**才提示（单条不报，避免误伤正常写法）。
- 状态：已做

## 六、待做（还没做，按我判断的疼度排序）

**R-30 上帝域 / 上帝模块** —— 某个域被越来越多域依赖，或某个文件被几十处引用；改一个签名动全仓。现在只拦"没人引用"（死代码），**不拦"被引用过多"**。状态：待做（下一轮）

**R-31 状态纪律不统一** —— 一半用状态库的 store、一半用 `useState + Context`；新人不知道跟哪套。状态：待做

**R-32 权限判断散落** —— 同一套"有没有权限"的判断写在十几个组件里，权限模型改一次要全仓找，漏的那处就是越权入口。状态：待做（最容易误伤，做之前要先定"哪些写法算合法展示判断"）

**R-33 路由守卫重复写** —— "未登录跳登录"在多个页面各写一遍，写法还不一致。状态：待做

**R-34 迁移边界只出不进** —— 待迁走的旧目录加进来之后，新代码也开始依赖它，于是它从"待清理"变成"事实核心"，迁移永远做不完。状态：待做

**R-35 公开面破坏性变更** —— 库发版时删了一个导出、改了签名，下游编译全崩，而门禁说"结构命名都对"。状态：待做（本期只做前端应用，排在后面；且要先划清与"不提供存量豁免"的界限）

**R-36 monorepo 跨包** —— 应用包绕过内部包的公开面直接引用源码；内部包反过来依赖应用包；跑门禁要一个个包手动跑、没有统一结论。状态：待做（大件）

**R-37 工程质量三件（不属架构）** —— ① 委派给 lint 生态的检查必须真的在跑（没装工具就等于失去那层覆盖，现在只有文档里写着）；② TypeScript 严格度（`strict` 之类的开关缺没缺）；③ 聚焦测试残留（`it.only` 会让其余测试静默不跑）。状态：待定

## 七、不做（写在这里是为了不再重复讨论）

- **SSR / 服务端渲染相关的边界**：本期不考虑。
- **联网判断一个包成不成熟**（下载量、是否废弃）：只做本地对账，成熟度是评审判断。
- **存量豁免 / 基线机制**：已移除，不复活（见 R-28）。
- **口味类判断**：抽象该不该合并、组件该不该拆、命名好不好、翻译质量、视觉品味 —— 那 review 的事，不做成红线。
- **构建与体积门禁**：本体不跑构建（零副作用）；包体积交给专门的工具。
- **`--watch` / `--fix` / 每条规则的文档链接**：判据等价即可，不追这些使用体验。

---

## 附：这份文档与其他文档的分工

| 文档                            | 回答什么                                       | 不回答什么                   |
| ------------------------------- | ---------------------------------------------- | ---------------------------- |
| **本文件**                      | 要解决什么痛点、期望行为是什么、做没做         | 怎么实现                     |
| `docs/agents/scenario-first.md` | 需求对话怎么进行（只讲场景）                   | 具体需求                     |
| `docs/DESIGN.md`                | 设计：判定等级、机制、边界与取舍               | 需求优先级                   |
| `.scratch/<slug>/spec.md`       | 单个需求的实现规格（验收标准、边界、Comments） | 需求全貌（那在本文件）       |
| `README.md`                     | 对外的能力概览与 Roadmap 进度                  | 需求的增删改（以本文件为准） |
| `CHANGELOG.md`                  | 每个版本实际发生了什么                         | 计划                         |
