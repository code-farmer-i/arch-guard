# 架构门禁范式（PARADIGM）

> 这份文档是**规约本身**，不是某个项目的说明。换仓库时整篇可搬，只需要替换 `arch.config.mjs` 与实例说明。
> 完整设计与理由见 [`docs/DESIGN.md`](./docs/DESIGN.md)。

## 0. 一句话

把架构从「文档里的原则」变成**可判定的不变量**：依赖只向下、真相只一处、层内聚合、跨层走接口、退路不留。

## 1. 三条公理

1. **真相唯一** —— 一个概念只有一个出处，其余地方只能引用，不能复写。
2. **依赖单向** —— 层只向下依赖；跨模块只走公开面；不许有环。
3. **退路不留** —— 类型逃生舱、调试残留、未完成标记、假数据、吞异常一律无豁免。

## 2. 可判定性等级（红线准入线）

| 等级              | 依赖什么信息       | 能判定的事                                            | 可信度         |
| ----------------- | ------------------ | ----------------------------------------------------- | -------------- |
| **L1 路径**       | 路径字符串         | 目录白名单、深度、槽位、命名、文件配对                | 100%           |
| **L2 单文件 AST** | 一个文件           | import 前缀、导出形态、字面量与上下文、语法红线、体积 | ~100%          |
| **L3 文件图**     | import 图 / 引用图 | 依赖方向、模块隔离、公开面、可达性、唯一出处、死物    | ~100%          |
| **L4 类型**       | tsc Program        | 类型级契约                                            | 高（要开开关） |
| **L5 语义**       | 人的理解           | 「是否业务中立」「命名好不好」「该不该拆」            | **判不了**     |

**铁律：`error` 级红线只准落在 L1–L3。** L5 一律不写进红线，只列进「门禁不做的事」。这条由引擎代码强制（`createRule` 会在 error + L4 时直接抛错）。

## 3. 十个检测原语（分类词汇）

这十个原语是**给规则分类的词汇**，不是引擎里的一层：每条规则都能说清它是哪个（或哪几个）原语的实例，所以
「不为每个项目另写一套」这句话有共同语言可讲。**实现里没有 `primitives` 模块**，规则直接消费事实模型
（见 §3.1）—— 为词汇造一层代码而无人消费，正是本范式自己禁止的「声明必须有消费者」。

| #   | 原语                 | 回答的问题                              |
| --- | -------------------- | --------------------------------------- |
| 1   | `forbidDependency`   | 谁不许依赖谁（层 / 模块 / 第三方包）    |
| 2   | `requireEntryPoint`  | 这个模块的公开面是什么                  |
| 3   | `exclusiveOwner`     | 这个能力 / 字面量只许在哪出现           |
| 4   | `mustReference`      | 别处必须引用常量 / 令牌，不许复写字面量 |
| 5   | `twinDeclaration`    | 必须同步的多份声明是否一致              |
| 6   | `referenceIntegrity` | 定义↔引用闭合：无悬空、无死物           |
| 7   | `onlyComponentIn`    | 某能力只许挂在指定位置                  |
| 8   | `forbidSyntax`       | 语法 / 形态红线                         |
| 9   | `limit`              | 体积与接口宽度                          |
| 10  | `metric`             | 数值断言（对比度等内置求值器）          |

外加两个图能力：`reachability`（无孤儿）、`cycles`（无环）。

> 颜色 / 路由 / 持久化 key / 环境变量 / **魔法数字** / 文案 key —— 都是 `mustReference` 的实例，共用一张「语义槽表」。

### 3.1 可机检的锚点在哪（不是原语本身）

「有分类」不构成保证 —— 保证来自两个**代码强制**的锚点：

1. **规则契约**：`createRule()` 在定义时就拒绝「id 与域不匹配」与「error 级落在 L4/L5」（红线只落 L1–L3）。
2. **每条规则一对夹具**：「违规必报 × 合规不报」，由 `--self-test` 跑（本仓 56 条规则 / 56 条全覆盖）。

为什么不做成语义原语层：原语一旦变成可执行 DSL，就必须证明它仍然只落在 L1–L3（那是 §2 的整套论证），
而规则是「对事实模型的纯函数」这件事已经做到了同一件事 —— 换 parser 不动规则、规则不碰 AST。
决定与理由见 [`docs/adr/0006-primitives-are-vocabulary.md`](./docs/adr/0006-primitives-are-vocabulary.md)。

## 4. 四张表 + 一张适配表

1. **角色表**：角色 → 路径 glob → 允许的导出形态 → 依赖档位
2. **公开面表**：模块 → 唯一入口
3. **语义槽表**：概念 → 唯一出处 → 触发形态
4. **阈值与白名单表**：体积、允许依赖、结构性豁免
5. **适配表**：组件库 / 数据层 / 路由 / 样式 / i18n 的可替换面

## 5. 五条门禁友好设计律

| 律                        | 内容                                                              |
| ------------------------- | ----------------------------------------------------------------- |
| D1 **路径即角色**         | 角色由路径唯一确定，不必读内容                                    |
| D2 **槽位按依赖档位定义** | 槽位的定义是「能依赖什么 / 能导出什么」，不是「放什么语义的东西」 |
| D3 **封闭枚举**           | 合法路径形态穷举，未登记即红（白名单 > 黑名单）                   |
| D4 **契约同构配对**       | 必须同步的多份声明放同一目录、命名同构                            |
| D5 **图必须可读**         | 禁 barrel/`export *`、禁相对越级、别名统一                        |

**形式化判据**：存在一张角色表，使源码树下**每个文件恰好命中一个角色**，且判据只用「路径 + 导出形态 + 依赖档位」。命中 0 个 = 结构不完备；命中 ≥2 个 = 有歧义。这条判据本身可机检。

## 6. 目录契约

### 6.1 三根拓扑

```
src/
├── app/        装配：唯一「全知」层
├── modules/    业务域：互不相识，各自自治
└── shared/     跨域共享：严格线性层序，不得含域语义
```

**域内 import 规则塌缩成一句话**：`./`、`@/modules/<自己>/…`、`@/shared/…`、第三方包。这是三根拓扑相对平铺的核心收益 —— 规则短到能背下来，agent 不会记错。

### 6.2 `shared` 线性层序

只许向层号 ≤ 自己的层 import；同级允许；全图禁环。

| 层号 | 目录              | 职责                                     |
| ---- | ----------------- | ---------------------------------------- |
| 0    | `styles` `assets` | 无代码依赖                               |
| 1    | `lib`             | 纯函数，无副作用、无项目语义             |
| 2    | `config`          | 项目常量：环境 / 路径 / 持久化 / 导航    |
| 3    | `i18n`            | 文案资源与初始化                         |
| 4    | `api`             | 契约与传输（禁 React / 组件库 / 状态层） |
| 5    | `stores`          | 客户端状态快照                           |
| 6    | `theme`           | 主题装配与令牌解析                       |
| 7    | `hooks`           | 跨域编排（**唯一的数据取数挂载点**）     |
| 8    | `components`      | 通用 UI                                  |

### 6.3 域模块契约

固定七个槽位：`routes.tsx` + `index.ts`（**两个公开面**）、`views/`、`components/`、`hooks/`、`model/`、`lib/`、`assets/`。

- **两个公开面，职责分开**（[ADR-0007](./docs/adr/0007-two-public-faces-per-domain.md)）：
  `routes.tsx` 给 `app` **挂路由**；`index.ts` 是**业务公开面**（实体 / 工具 / 类型）给**别的域**用。
  `views/` 仍然对域外私有（页面不外借）→ 域整体仍是深模块，只是多了一个**显式**的业务出口。
  为什么翻掉旧结论（"只留 `routes.tsx`"）：实测跨域**没有合法通道**时，东西只能抬进 `shared` ——
  而 `shared` 会长成"第二套 modules"（端点 / DTO / 键 / 策略全在那儿，几十个域时是横向 merge 队列）。
- **域外引用只许落在对方的公开面**：`import '@/modules/<域>/routes'`（挂路由）或
  `import … from '@/modules/<域>'`（业务）；直捣 `views/` / `components/` / `model/` / `lib/` 一律红（S04–S06 + S23）。
- **声明二选一**：`structure.publicApi: ['domain']`（**推荐** —— 跨域经入口，直捣照报）或
  `structure.isolate: ['domain']`（同层互引一律禁；那时跨域只能靠 `app` 组装或抬进 `shared`）。
- `index.ts` **只导出实体 / 工具 / 类型**，不导出实现细节（两个门已经够多了，别让第二个门变成"什么都能拿"）。
- 加一个域的成本 = 建目录 + 两份文案 + 路径常量 + 装配层一行聚合，**全是加法，不改任何已有域**。

### 6.4 唯一落点与反例

| 要写的东西                           | 唯一落点                                                                                                 |
| ------------------------------------ | -------------------------------------------------------------------------------------------------------- |
| 页面                                 | `modules/<域>/views/<名词>Page.tsx`（default 导出）                                                      |
| 域内组件 / 哑基础件 / 业务中立组合件 | `modules/<域>/components/` / `shared/components/ui/` / `shared/components/common/`                       |
| 跨域编排 / 域内编排                  | `shared/hooks/use<资源>.ts` / `modules/<域>/hooks/`                                                      |
| 端点 / 契约类型 / query key          | `shared/api/<资源>.ts` / `types.ts` / `queryKeys.ts`                                                     |
| 路由路径 / 路由分片                  | `shared/config/paths.ts` / `modules/<域>/routes.tsx`                                                     |
| 持久化 key / 环境变量                | `shared/config/storage.ts` / `shared/config/env.ts`                                                      |
| 文案                                 | `shared/i18n/locales/<lang>/<命名空间>.ts`                                                               |
| 组件私有样式                         | 同目录同名 `*.module.css`                                                                                |
| 全局 Provider 套壳                   | `app/App.tsx`（一层层 `<XProvider>` 嵌套）；**provider 的配置对象各自回家**：api / theme / i18n / stores |
| 全局 Provider 的配置对象             | `shared/api/`（queryClient）/ `shared/theme/` / `shared/i18n/` / `shared/stores/`                        |

| 错误落点                            | 正确的家                                          | 为什么                                       |
| ----------------------------------- | ------------------------------------------------- | -------------------------------------------- |
| `src/utils/` `src/helpers/`         | `shared/lib`                                      | 顶层只有三根                                 |
| `src/services/`                     | `shared/api`                                      | 传输只有一个家                               |
| `shared/components/<域>Panel.tsx`   | `modules/<域>/components/`                        | 带域语义的东西不许进 shared                  |
| `modules/a/` 被 `modules/b/` import | 提升到 `shared/components/common` 或由 `app` 组合 | 域间零依赖                                   |
| 单域专用的 hook 放 `shared/hooks/`  | 下沉到该域                                        | 只有跨域复用才提升                           |
| `../..` 相对越级                    | 别名 `@/…`                                        | 一条 import 规则                             |
| 新目录 `src/features/`              | 不存在这个位置                                    | 目录白名单                                   |
| `app/providers.tsx`（React 习惯）   | 套壳写进 `app/App.tsx`，配置下沉 `shared/`        | app 是唯一全知层；范式不为框架习惯开角色口子 |

### 6.5 契约扫描域（`include`）

角色表回答「契约内怎么落位」，但**不回答「哪片树属于契约」**。`include`（配置根相对 glob）补上这一句：

- 只有命中 `include` 的 ts/css 参与角色判定与逐文件规则；域外的 `vite.config.ts` / `e2e/` / `scripts/` / 生成代码
  既不报「不在目录契约内」，也不被逐文件规则扫。
- 域外文件**仍进依赖图**（照常解析，角色记为 `(outside)`）：import 边与「测试是独立可达根」都靠 facts，
  少了它们，只被域外测试引用的文件会被误判成孤儿。
- 预设给默认：`canonical()` / `library()` 都是 `[<srcRoot>/**]`；空数组 = 不限制（引擎默认）。
- 与 D3「封闭枚举」不冲突：`include` 决定**哪片树**参与枚举，角色表决定枚举**怎么落位**，且 `include` 只能更粗。
- 报告必须自述扫描域与域外文件数 —— 收窄扫描域是行为变更，不许静默。

**`ignore` 与 `include` 是两件事，别混**：

|           | 含义                                                                         | 会被解析吗    | 会用在哪                         |
| --------- | ---------------------------------------------------------------------------- | ------------- | -------------------------------- |
| `ignore`  | **别碰**：完全不属于这个项目（构建产物、示例宿主、夹具）                     | ❌ 不进文件集 | 连图都不进                       |
| `include` | **不判契约**：不属于源码树，但可能与依赖图有关（`tests/`、`vite.config.ts`） | ✅ 照常解析   | 进图，供跨域 import 与测试可达根 |

只写 `include` 而把构建产物留给它 → 那些文件**照样会被解析**（实测：本仓库狗粮从 71 个文件涨到 340 个）。
两者属于不同层：`ignore` 是「项目边界」，`include` 是「契约边界」。

**「别碰」分三层，叠加生效**（谁都没被谁替代）：

| 层              | 来源                                       | 管什么                                                                                                 |
| --------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------------------ |
| ① 宿主 `ignore` | `arch.config.mjs`（**显式**）              | `__fixtures__/` / `examples/` / `.scratch/` 这类**被提交但不属于源码树**的树 —— 只有宿主知道           |
| ② `.gitignore`  | **问 git**（基础层，自动）                 | 生成代码 / 临时脚本 / 构建产物：`.git/info/exclude`、全局 excludes、子目录各自的文件、`!` 否定全都算数 |
| ③ 通用产物目录  | 数据表 `data/build-output-dirs.ts`（兜底） | `node_modules` / `dist` / `.next` / `.nuxt` / `.svelte-kit`… —— 没有 git 时也能用                      |

三条都要守住的规矩：

- **第 ② 层只作用于契约域外**。契约域内（`include` 命中）的文件即使被 `.gitignore` 也照判 ——
  宁可吵（生成物被报出来，宿主去 `ignore` 里显式声明），也不静默不判（那是最难发现的假绿）。
  **推论：契约扫描域为空（不限）时这一层不生效** —— 「域外」这个概念本来就是 `include` 定义的，不是 VCS 定义的。
  预设（`canonical()` / `library()` / `fsd()`）都已声明 `include: [<源码根>/**]`，所以只有**自写预设**才会缺；
  缺了也不是静默的：域外文件未命中角色会直接报 S01「无处安放」（吵，但指得出文件）。
- **`!` 与嵌套由 git 判定，不自己解析**：否定、锚定、子目录各自的 `.gitignore`、`.git/info/exclude`、全局 excludes ——
  自己写错任何一条的后果都是"多跳了 = 静默不判"。顺带记住 git 自己的语义：**被忽略的目录内部救不回文件**，
  要留一个就得写 `gen/*` + `!gen/keep.ts`。
- **跳过什么必须自述**：`ignore（项目边界）命中 N 个文件`、`因 .gitignore（git 判定）跳过 N 个文件`。
  没有 git（或不是仓库）时第 ② 层**整层降级关闭**，行为回到 ① + ③。

### 6.6 结构声明：范式是可换的数据

上面五节写的是**我们这套范式**（三根、线性层序、域契约、唯一落点、扫描域）。但"目录规范"这件事本身是**可声明的数据**，
不是本体的硬编码 —— 库 / FSD / Atomic Design 都能用同一份引擎表达：

| 声明                             | 含义                                                 | 判它的规则 |
| -------------------------------- | ---------------------------------------------------- | ---------- |
| `structure.order: true`          | 层序单向：只许依赖**层号 ≤ 自己**的文件              | S21        |
| `structure.isolate: ['slice']`   | 组隔离：同维度、**同层**、不同组之间不许互相引用     | S22        |
| `structure.publicApi: ['slice']` | 公开面：组必须有入口；组外不许直接引用组内非入口文件 | S23        |
| `structure.couplingLimits`       | 组耦合上限：被多少个其它组依赖 / 依赖了多少个其它组  | S39        |
| `structure.migrating`            | 迁移中的路径：外面不许引用它们（它们引用别处是目的） | S40        |
| `structure.clientState`          | 客户端状态单元的命名形态 + 落点                      | S41        |
| `structure.authRedirects`        | 跳转守卫的登录路径 + 落点                            | S42        |
| `structure.maxRelativeUp`        | 相对越级的层数上限（`../` 最多爬几层）               | S43        |
| `structure.generated`            | 生成物路径：这些文件必须带 `@generated` 标记         | H13        |

**组**与**入口**都写在角色表里，规则不认识任何具体文件名：

```js
roles: [
  { id: 'pages:ui', pattern: 'src/pages/{slice}/ui/**', layer: 5, group: 'slice' },
  {
    id: 'pages:index',
    pattern: 'src/pages/{slice}/index.{ts,tsx}',
    layer: 5,
    group: 'slice',
    entry: true,
  },
]
```

三条声明**各有一条规则消费**（没有消费者就不该有声明 —— `config.layers` 那次教训）。
应用范式（三根）同样声明 `structure: { order: true }` —— **一套层序机制管到底**（原先 shared 专属的 S07 已并入 S21）。
域与装配层的**关系**（域间只能经 routes、layouts 不许引域、有 views 必有 routes）仍归 S04–S09 与 S14：那些不是层序，声明表达不了。

本体自带三个范式预设：`canonical()`（三根应用）、`library()`（库 / CLI）、**`fsd()`**（Feature-Sliced Design）。
换范式就是换预设那一行 —— 引擎、规则、豁免规则都不动。**一个配置只许一个范式**（混用直接报错）；
域预设（设计系统 / 文案 / 依赖 / 度量）可以自由叠加，且**契约落点跟着范式走**：
范式声明自己惯用的目录，域预设只写你显式给的路径。要加项目自己的目录用 `addRoles`（追加），不必整份重写角色表。

**边界**：只能表达规范里**可判定**的那部分（L1–L3）。"这个 feature 必须是一个用户动作"（FSD）、
"这个组件是 molecule 还是 organism"（Atomic Design）是 L5，任何引擎都判不了 —— 这类留给 code review 和文档。

## 6.11 权限点与租户上下文：各有一个落点（R-110）

| 落点             | 声明                                                                                                                | 判据                                                      |
| ---------------- | ------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------- |
| **权限点表**     | `permissions({ apis: ['can'], source: 'src/shared/auth/permissions.ts' })`                                          | 调用点写 `can('crew:edit')` 字面量即报（D29）；传常量合规 |
| **判断写在哪儿** | `callSites([{ name: '权限判断', apis: ['permissions.includes'], in: ['src/shared/auth/**'] }])`                     | 判断散在组件里即报（S38 / R-46）                          |
| **租户从哪儿拿** | `callSites([{ name: '租户上下文', apis: ['searchParams.get'], args: ['tenantId'], in: ['src/shared/tenant/**'] }])` | 别处解析租户即报；`args` 让 `get('page')` 不误伤          |

为什么 `args` 是必要的：`searchParams.get` / `cookies.get` 这类 API 名字**太泛** —— 只看调用名会把
`?page=` / `?tab=` 全判红；只看实参名又认不出调用。两个条件一起才是"这一类调用"。

项目自己的槽位（如 `shared/auth`、`shared/tenant`）用 `overrides.addRoles` 声明 —— 注意它在
**overrides 层**（与 `structure` 平级），放进 `structure` 会静默不生效。

## 6.12 失败处理的策略：形态也有落点（R-111）

`retry: 3` 这类**数字**归 D20（`numberHomes`）；**形态**归 D30：

| 写法                                                                                   | 谁管                                         |
| -------------------------------------------------------------------------------------- | -------------------------------------------- |
| `retry: 3` · `staleTime: 60_000`                                                       | D20（数字型策略有家）                        |
| `retry: (count, err) => count < 3` · `backoff: 'exponential'` · `retryDelay: (i) => …` | **D30**（函数型 / 枚举型策略只在声明的落点） |
| `retry: RETRY_LIMIT`（引用）                                                           | 不管（没有字面量可判）                       |

```js
errorPolicy({ policyIn: ['src/shared/api/policy.ts', 'src/modules/<域>/model/query.ts'] }),
```

策略**名字**可以不写：装了 `dataLayer(reactQueryKit(...))` 就由它的 `policyProps` 给（库的事实不用抄）。

**边界**：只判"策略对象**作为调用实参**"（`useQuery({ retry: … })`）。`retry` 这个名字会撞 ——
UI 回调、i18n 的文案键都可能叫 `retry`，不收窄就会误报（这条边界是示例里的真实误报逼出来的）。

## 6.13 测试分层：每层一个落点（R-112）

单测 / e2e / 契约测试**是三件事**，却常常混在同一个 glob 里 —— CI 分不清快反馈与慢验证，
e2e 直接 import 源码内部（重构就红），契约漂移没人发现。

```js
metrics({
  tests: {
    homes: [
      { name: 'unit', glob: 'src/**/*.test.ts' },                                   // 默认：不限制"从哪进"
      { name: 'e2e', glob: 'e2e/**/*.spec.ts', imports: 'public' },                  // 只许经公开面 / 应用入口
      { name: 'contract', glob: 'tests/contract/**', mustImport: ['src/shared/api/generated/**'] },
    ],
  },
}),
```

配套三件事（少一件这条纪律就空转）：

1. `include` 要覆盖测试层（`['src/**', 'e2e/**', 'tests/**']`）；
2. 每层一个**角色**（`addRoles: [{ id: 'e2e', pattern: 'e2e/**', layer: 99 }]`）；
3. 应用入口进 `entries`（`app:bootstrap` 只是 slot，没标 `entry`）。

## 6.14 配置写错会在配置期报错（R-113）

声明型配置最怕"写了、看着对、实际没生效"。所以：**不认识的键直接拒**，并列出可用键。

真正踩过的形态：`addRoles` 放进 `overrides.structure`（键名对、值是好的，只是放错一层）——
引擎静默忽略，S01 照旧报"域根散件"，排查时间全花在错的地方。现在会直接说：

```
overrides.structure 里有不认识的键：addRoles
提示：结构声明写在 `structure` 里；`addRoles` 在它**外面**（overrides 层）。
```

| 层                    | 可用键从哪来                                                                                        |
| --------------------- | --------------------------------------------------------------------------------------------------- |
| 顶层                  | `specVersion` / `presets` / `packs` / `overrides`                                                   |
| `overrides`           | `Config` 的键（`include` / `ignore` / `entries` / `structure` / `params` / `enable` …）+ `addRoles` |
| `overrides.structure` | `StructureSpec` 的键（层序 / 隔离 / 公开面 / 各种阈值…）                                            |
| 适配器字段            | 各 facet 的白名单（`defineAdapter` 拒未知字段）                                                     |

白名单用 `Record<keyof X, true>` 写 —— **类型加了字段这里就编译不过**，不会悄悄落后于契约。

## 6.15 后端端点的唯一出处（R-99 / R-114）

```js
// 形态一（集中）：一个后端、接口变更要整体审阅
endpoints({ from: callSiteSources.platform.network, source: 'src/shared/api/endpoints.ts' }),

// 形态二（跟域走，R-116）：多团队各管一块后端 —— 每加/下线一个域都不必动共享文件
endpoints({ from: callSiteSources.platform.network, source: ['src/modules/<域>/model/endpoints.ts'] }),
```

**三个问题，按顺序问**（第一个不决定性时问下一个）：

1. **谁发起下一次变更**：外部（后端 / SDK / 分析团队）且一次改多个域 → 集中；本域功能迭代天天改 → 跟域走。
2. **加一个域是"加文件"还是"改共享文件"**：加新文件（如 `locales/<语言>/<域>.ts`）可以接受；
   改一个共享登记表（如往 `ENDPOINTS` 里加一行）只在问题 1 答"外部批量"时才划算。
3. **这个名空间是全局的还是局部的**：URL 空间 / i18n 键空间 / 事件名空间是**全局**的（两个域不能各自
   决定 `/crews` 归谁）→ 必须集中；缓存键 / 分页大小 / 领域模型是**局部**的 → 跟域走。

**示例按这三问过了一遍的结论**（2026-09-25 实测）：

| 东西                                            | 三问的答案                                  | 结论                                        |
| ----------------------------------------------- | ------------------------------------------- | ------------------------------------------- |
| 后端端点 `shared/api/endpoints.ts`              | 外部批量 · 改登记表 · 全局名空间            | **集中**（多团队分治时改跟域走，R-116）     |
| DTO `shared/api/types.ts`                       | 外部批量 · 改登记表 · 映射在前端            | **集中**（mapper 跟域走，A2）               |
| 埋点事件名 `shared/lib/analytics/events.ts`     | 外部（分析团队）· 改登记表 · 全局名空间     | **集中**                                    |
| 路由 `shared/config/paths.ts`                   | 前端发起，但**URL 空间是全局的**            | **集中**（D23 的唯一出处）                  |
| i18n `locales/<语言>/<域>.ts`                   | 前端 + 产品发起 · **加新文件** · 键空间全局 | **集中目录 + 分域文件**（加域不改已有文件） |
| 缓存键 / 请求策略 `modules/<域>/model/query.ts` | 本域迭代 · 加文件 · 局部                    | **跟域走**（R-97/A3）                       |
| **分页大小**                                    | 本域 UX · 加文件 · 局部                     | **跟域走**（R-117 —— 之前漏在 shared 里）   |

**一条判据：看这份知识的「下一次变更由谁发起、多频繁」** ——

| 变更由谁发起                                          | 放哪                       | 例子                                                                             | 为什么                                                                               |
| ----------------------------------------------------- | -------------------------- | -------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------ |
| **外部**（后端 / SDK / 分析团队），且一次改**多个域** | **集中**（一个契约登记处） | 后端端点路径 · DTO · 埋点事件名 · 环境变量名                                     | 一次后端发布对应**一个 PR 可整体审阅**；合并压力跟随后端发布，而不是跟随前端功能迭代 |
| **本域的功能迭代**，天天改                            | **跟域走**                 | 缓存键 · 请求策略 · 领域模型与 mapper · 域内状态                                 | 域自治：加/下线一个域**不必动共享文件**（共享文件的每次改动都是横向 merge 队列）     |
| 迁移中（两边都有）                                    | 数组/glob 同时声明         | `source: ['src/shared/api/endpoints.ts', 'src/modules/<域>/model/endpoints.ts']` | 先立中央基线，再逐域搬；不用一次性大爆炸                                             |

**不变的一条**：同一份知识**只能有一个家**（不管选集中还是跟域）—— 这正是 D22–D26 那一族门禁在管的事。

**端点的结论**：一个后端整体审阅 → 集中；按域分治（多团队各管一块后端）→ 跟域走。`source` 两种都收（数组 + glob）。

对照：**缓存键 / 请求策略**是**前端自己的约定** → 一律跟域走（R-97/A3，见 §6.9 旁注），
**埋点事件名**是外部数据契约 → 集中（`shared/lib/analytics/events.ts`）。

- 打后端的调用（`fetch` / `XMLHttpRequest`，或 `apis: ['axios.get']` 自列）实参里出现**路径字面量** → 报（D25）；
- `apis` 手写时，拼错一个字母会被"声明 0 命中"点名；用 `from` 走来源表则**不报**（既有清单天然含没用到项）。

## 6.16 `apis` 怎么匹配调用名（R-115）

`callSites` / `endpoints` / `permissions` / `analytics` / `errorPolicy` 里的"名字清单"按同一套形态匹配
（**全仓唯一实现** `src/engine/api-match.ts`，规则与"声明 0 命中"自述共用）：

| 声明                | 命中的调用                           | 为什么                                       |
| ------------------- | ------------------------------------ | -------------------------------------------- |
| `fetch`             | `fetch(...)`                         | 整名                                         |
| `localStorage`      | `localStorage.getItem(...)`          | **对象前缀**（内置对象的方法名不必逐个声明） |
| `invalidateQueries` | `queryClient.invalidateQueries(...)` | **方法后缀**（接收者变量名由项目决定）       |

三条边界：

1. **不是子串**：`can` 不会匹配 `cancel` / `candidate`（都带 `.` 或整名边界）；
   `fetch` 不会匹配 `fetchX`。
2. **同一调用命中多个声明时取声明顺序里的第一个**（可预期，不随机）。
3. **泛名字的"同名不同义"用 `args` 收窄**，不要指望匹配形态替你分辨 ——
   `searchParams.get` 会匹配 `?page=` 也会匹配 `?tenantId=`，所以声明
   `args: ['tenantId']`（R-110）。这条与 N-10 判不做 feature flag 的结论一致。

## 6.17 报告还会给"架构建议"（R-118，不阻断）

门禁除了"违规"还会给**建议**：从已有事实算出的**可核对信号** + 处方选项，**不影响退出码**。
目前两条：

| 建议               | 信号                                                               | 提示什么                                           |
| ------------------ | ------------------------------------------------------------------ | -------------------------------------------------- |
| **各归各域**       | 一个文件里 ≥3 个导出各自只被一个域用                               | 这些东西跟域走更省心（判据见 §6.15）               |
| **组粒度**         | 某个组只有 1 个源文件 / 超过 20 个                                 | "域"这个词失去意义 / 一个组装了俩业务              |
| **未测试的逻辑组** | 组里有逻辑（`lib`/`model`/`api`/`hooks` 的非 JSX 文件）却 0 个测试 | 补测试并纳入 `requireTestsFor`                     |
| **同层组复用**     | 一个组被 ≥3 个同级或更内层的组引用                                 | 它已是公共依赖：提升到共享层，或声明它就是这个地位 |
| **组级依赖环**     | 组边图里的环（S08 只见文件级环）                                   | 共用部分下沉 / 一侧单向依赖                        |
| **组间依赖链过深** | 链上 ≥4 个组（宽度归 S34/S39，**深度**以前没人管）                 | 中间那组提升为共享层，让两端直接依赖公共件         |

建议**不是错**：规则管"违规 → 红"，建议管"判断 → 你看"。**在已知健康的样板上必须 0 条**（验收惯例）。

确认某条建议不适用时用 `overrides.adviceAllow` 声明豁免（形状同 `exceptions`：信号 + 主体 glob +
理由必填 + 到期必过期），**报告里会自述被豁免了几条** —— 静默关闭是这套机制最该防的事。

## 6.18 错误码 → 文案：推荐形态（**本体不判**）

```ts
// 唯一映射表：错误码 → 文案 key（一处）
export const ERROR_MESSAGES = {
  RATE_LIMIT: 'common.errorRateLimit',
  NOT_FOUND: 'common.errorNotFound',
}
export function messageOf(code: string | null): string {
  /* 不认识就回退通用文案 */
}

// 取数层只暴露一次错误码，页面不碰 `err.code`
const { errorCode } = useCrews()
t(messageOf(errorCode))
```

反例（**本体目前不判**，别这样写）：`if (err.code === 'RATE_LIMIT') setMsg('太频繁了')` —— 同一个错误码
在三个页面各写各的说法，客服拿着日志对不上。

**为什么不立判据**：判据必须贴真实写法（错误码是字符串还是数字、判断在组件还是 api 层、文案是 key 还是
中文、表是对象还是 `Map`），没有真实项目提出这个场景就落 = 误报机（同 N-12 / N-14 / N-15）。
形态照演进示例（两套都演），**判据留待**。

## 7. 例外：规则级，且必须指名

- **违规没有存量豁免**：没有基线、没有"先记下来以后再说"。门禁的结论只有两种 —— **符合规范** 或 **不符合**。
  这是刻意的取舍：只要存在"一键把当前违规记下来"的通道，它就会变成日常动作，而不是例外。
- 唯一的宽松通道是 **规则级例外**（`exceptions: [{ rule, glob, reason, expires? }]`）：
  它声明的是「**这条规则**对**这类文件**不适用」——**不是**「这个文件免检」。
  - `rule` 必须真实存在（拼错 → 直接报错，因为"例外没生效"是假绿）；
  - `reason` 必填（进 `arch.config.mjs` 的 diff，可评审）；`expires` 可选，写了就**过期即红**（逼你续期或删掉）；
  - 实现是**对发现项做后置过滤**：文件照常有角色、进依赖图、被其它规则判定，只有指名的那条规则被摘掉。
  - 每次运行都会**点名**每条例外（命中几处 / 未命中），未命中的提示"可能可以删掉"——例外必须透明。
- **三种需求别混用**：① 这片树不属于契约 → `include` / `ignore`；② 这条规则对它不适用 → 这里；
  ③ 暂时不想修 → **没有这个通道**。
- **没有内联豁免注释**（连 `eslint-disable` 这类注释本身都是红线 H02）。
- **别和覆盖率棘轮混淆**：M04「覆盖率不许倒退」比的是覆盖率快照（`--update-coverage` 维护），它不豁免任何违规。

### 7.1 「本地调试不就是要写 localhost 吗」

写 `localhost` 是本地调试的常态，但**源码里的地址不该是它唯一的家** —— H08 报的正是"非生产路径之外的本机 / 内网地址"。
判据不为此放宽；三条出口按需求分：

- **首选：地址进环境变量**（`import.meta.env.VITE_API_URL` / `.env` / 构建配置）。这条满足绝大多数情况，不需要任何声明。
- **dev-only 形态 → 登记成非生产角色**（Storybook 故事、dev-only 模块、`*.cy.*`）：
  `addRoles: [{ id: 'test', pattern: '**/*.stories.{ts,tsx}', layer: 99, exclusive: true }]`。
  注意是 **`addRoles`（追加）**，不是 `overrides.roles` —— 后者是**整体替换**角色表，写了它范式的角色就没了。
  这条"非生产"豁免被 H07 / H08 / H09 / C01 / S36 / S38 / S44 共用（它们判的都是"非测试路径"）。
- **确实要留的本地默认值**（唯一的地址出处，如 `src/shared/config/env.ts` 里的兜底）→ 规则级例外：
  `exceptions: [{ rule: 'H08', glob: 'src/shared/config/**', reason: '地址唯一出处（本地兜底值）' }]`；
  每次运行点名、可设 `expires`。这是"这条规则对它不适用"，不是"这个文件免检"。

## 8. 适配器：是数据，不是插件

通信是**单向契约 + 能力协商**，引擎从不反向调用适配器：

| 方向          | 机制                                                            |
| ------------- | --------------------------------------------------------------- |
| 适配器 → 引擎 | 工厂函数返回纯数据对象，经 `defineAdapter(面, spec)` 校验并冻结 |
| 适配器 → 规则 | 规则只读 `ctx.adapters.<面>`                                    |
| 引擎 → 适配器 | **不存在**：无回调、无 hook、无生命周期                         |

**能力协商**：规则声明 `requires: ['uiKit.vendorSelectors']`，未声明的能力对应规则**不注册**，并在报告里明列「因能力未声明而停用」—— 避免「以为在跑其实没跑」。

`defineAdapter` 做字段白名单 / 类型 / 正则可编译性 / 冲突校验；模式类字段还必须自带命中/不命中样例，由 `--self-test` 跑。**用户配置只能填数据，要写逻辑就把适配器贡献进本体。**

### 8.1 反向不变量：声明必须能被项目事实证伪

能力协商只保证「**没声明**就不注册」；它管不了另一半 —— **声明了却没有对应事实**。那时规则会安静地遍历空集合，
门禁显示「通过」，而这几条规则一条都没跑。所以每个可声明的面都必须配一条**事实存在性**判据：

| 声明                  | 必须存在的事实                                 | 规则                      |
| --------------------- | ---------------------------------------------- | ------------------------- |
| `copy()`（i18n）      | `resourceDir` 下至少一个文案文件               | C07（warn）               |
| `designSystem()`      | `paletteFile` / `tokenDir` 至少匹配到一个 CSS  | D21（warn）               |
| `uiKit(kit)`          | 至少 import 一个声明的包，或出现 vendor 选择器 | P11（warn）               |
| `metrics()`（覆盖率） | 产物存在，且不比最近一次提交旧                 | M06（error，fail-closed） |

三条硬要求：

1. **区分「没声明」与「声明错了」**。没声明就安静（不打扰本来就不做这件事的项目）；声明错了必须报。
   当参数带默认值时（如 `designParams()` 内置的 `src/shared/styles`），需要一个**显式标记**才分得开
   （例：`params.designSystemDeclared`）。
2. **只报事实，不猜意图**。判据只能是「路径有没有匹配到文件」「图里有没有 import」这类可判定事实，
   不能是"看起来不像在用"（那是 L5）。
3. **委派出去的也要写明**。能力层的反向检查（P08「登记库必须真的被用」：登记了首选方案却全项目零引用）
   按 [DESIGN.md](./docs/DESIGN.md) §4.9 **委派给 knip / depcheck**，本体**没有实现 P08** —— 它是"已委派"，不是"没想过"；
   本体里真正在跑的反向检查是**适配表层**的 **P11**。

## 9. 检测范围的语义

> **scope 只过滤报告，不过滤正确性。**

| 谓词类型          | 例子                        | 能否局部判定  |
| ----------------- | --------------------------- | ------------- |
| 单文件形态 / 出边 | `any`、命名、体积、相对越级 | ✅            |
| 全图谓词          | 域隔离、层序、无环、可达性  | ❌ 必须全量图 |
| 全局唯一性        | 颜色唯一、死令牌、双份一致  | ❌ 必须全量   |
| 角色表完备        | 目录契约                    | ❌ 必须全量   |

实现分层：**facts 按文件缓存（增量），图与全局谓词每轮从 facts 重建**（O(边数)，毫秒级）。这样既快，又不会出现 stale-cache 假绿。

**安全语义**：不可归属的全局违规默认仍然失败；无 git / 空 diff 明确降级并提示；untracked 必须纳入、rename 按改名处理；pre-commit 读 index 内容；CI 必须 `full`。

## 10. 门禁不做的事（诚实边界）

门禁约束的是**形态**（放哪、能依赖谁、真相几份、有没有退路），不约束**质量**。以下一律不写进红线，靠 review 与设计判断：

- 域划分是否合理、两个域该不该合并
- 抽象是否重复、组件该不该拆（只能用体积与接口宽度阈值近似）
- 命名是否恰当（正则只查形态，查不了质量）
- 翻译质量、对比度之外的视觉品味
- 类型与后端契约是否真的吻合

> L5 语义判断的完整清单（哪些东西永远不写进红线）见 `docs/DESIGN.md` §5.6。

推论：**存在合法绕过空间**（把业务逻辑塞进一个「纯函数」文件即可通过大多数规则）。因此门禁必须与 review 配对；「判据可执行」与「规则命中统计」是把绕过变可见的关键。

## 11. 可扩展性三层

| 层              | 换什么                                                     | 形态                                                    |
| --------------- | ---------------------------------------------------------- | ------------------------------------------------------- |
| **配置**        | 项目专有事实（路径、令牌前缀、阈值、白名单）               | 数据表                                                  |
| **适配器**      | 同一元框架内的库（组件库 / 数据层 / 路由 / 样式 / i18n）   | **纯数据表**                                            |
| **框架包 pack** | **源码形态**（TS/JS 家族 / React(TSX) / Vue SFC / Svelte） | **代码**：parser + 角色表变体 + 语言相关规则 + fixtures |

**pack 怎么选**：宿主在 `arch.config.mjs` 里写 `packs: [tsPack]`（纯 TS 库 / CLI）或 `packs: [reactPack]`（React 应用）；
**一个项目只允许一个** —— 换形态是换 parser 与整套规则集，不是叠加。pack 自己声明 `framework`（= 源码形态），
它与配置里的 `metaFramework` 只能有一处真相（两边都写且不一致直接报错）。调用方（CLI）只提供**兜底包**，
引擎不认识任何 pack（依赖方向是 pack → 引擎，不能反过来）。

> **`framework` 表示源码形态，不是"用了哪个框架"。** 它实际只驱动两件事：哪些扩展名归本包管
> （其余形态的源码由 S20 报出）、报错文案里写哪个包。**没有任何规则按它分支** ——
> 所以纯 TS 项目不该被一个叫 "react" 的包量。v1 的两个 pack（`tsPack` / `reactPack`）
> **今天共用同一份规则集**（`packs/core/rules/`），因为还没有任何 JSX 专属的已实现规则；
> 出现时它们进各自 pack 的 `rules/`，那时两者才真正分化。

### 11.1 工程类型：应用范式 vs 库范式

同一套引擎要量两类工程，而它们的目录契约本就不同：

|             | 应用（`canonical()`）               | 库 / CLI（`library()`）                        |
| ----------- | ----------------------------------- | ---------------------------------------------- |
| 角色        | 三根拓扑：装配 / 业务域 / 共享      | **入口 + 项目自己声明的目录表**（目录 → 层号） |
| 公开面      | 域 = `routes.tsx` + 业务 `index.ts` | 包入口（默认 `index.ts`，可多个）              |
| 别名        | 跨目录必须走 `@/`（S10）            | 内部相对导入是 Node 生态常规，**S10 关闭**     |
| 业务规则    | 域路由必填（S14）、导出形态（S13）  | 不适用，随规则集一起关掉                       |
| 域 / 共享层 | `layout.modules` / `.shared`        | **置空 = 不存在**，依赖它们的图规则自然空转    |

**结论**：工程类型不是新概念，而是「预设贡献角色表 + 规则集」这一既有机制的第二次应用 —— 引擎一行没改，只是多了一份 `presets/library.ts`。这也解释了为什么不能让所有规则默认常开：**规则集必须跟着工程类型走。**

库的角色表由 `library({ modules: { utils: 1, core: 2 } })` 生成 —— 目录名是**项目事实**，不是本体能猜的；
不给目录表就只认入口，其余文件以 S01「无处安放」报出来（封闭枚举对库同样成立）。
本仓库自己也用同一份预设，只是把自己的目录表写出来（`data: 1, engine: 2, packs: 4, presets: 4`）。

**预设各自贡献自己的规则集，并取并集。** 域预设（设计系统 / 文案 / 依赖 / 度量 / 反退化）声明"我贡献哪几条"，
应用范式声明 `enable: 'all'`，库范式给白名单；**任一预设说 `all`，结果就是 `all`**。
要减掉某条用 `disable`（预设与 `overrides` 都取并集）。这样组合是加法：
`library({ modules }) + designSystem() + copy() + deps()` 自然得到"库规则 + D 域 + C 域 + P 域"，
不会像"后者覆盖前者"那样把某一边的域整块静默关掉。

**布局只有一处真相：`layout`。** 规则读 `layout.modules` / `layout.shared` 判断域根与共享根，
**不许再自己拼 `${srcRoot}/modules`** —— 拼死过一次：`canonical({ modules: 'src/features' })` 时角色表跟着变了，
图规则却还在查 `src/modules`，查不到就静默空转，跨域引用私有 views 一条都不报（假绿）。

**范式不变量不配置**：三根角色划分、唯一出处、依赖单向、无环、退路不留、目录白名单、红线只落 L1–L3、禁内联豁免。

**跨框架的边界**：目录契约（三根拓扑、角色表、依赖方向、公开面、唯一出处）与框架无关，**整篇可搬**；
parser 与语言相关规则属于 pack（换元框架 = 换 parser + 角色表变体 + 规则集变体）。
由此得一条不变量：**当前 pack 量不了的源码形态必须显式报错** —— 认不出的 `metaFramework` 直接拒绝执行，
项目里混进的 `.vue` / `.svelte` 由 S20 报出。「0 个文件 → 通过」是假绿，比报错危险。

## 12. 选型纪律：优先成熟开源方案

**「用 dayjs 还是手写」是 L5 判断，判不了。但它的四类后果全都可判定：**

| 可判定后果                           | 等级  | 规则                                                        |
| ------------------------------------ | ----- | ----------------------------------------------------------- |
| 依赖未登记 / 命中禁用库              | L1    | P01 / P02                                                   |
| 幽灵依赖（import 了却没声明）        | L3    | P03（已委派 knip）                                          |
| **能力被使用 ⇄ 首选库已声明/已使用** | L2+L3 | P06 / P07（能力层 P08 已委派 knip；适配表层反向检查是 P11） |
| **手工轮子的语法指纹**（已实现）     | L2    | P06                                                         |
| **手工轮子的命名指纹**（已实现）     | L2    | P07                                                         |

### 12.1 能力表 = 选型表的机读真相

```js
deps({
  capabilities: { datetime: 'dayjs', 'cli-args': 'commander', http: 'fetch' },
})
```

能力表写在 `arch.config.mjs`，`AGENTS.md` 里的选型表由它渲染（文档管理块），**杜绝两处真相**。能力表管的是「这个能力必须用哪个方案」（驱动 P06）；**依赖批准清单是另一件事**，由 `deps({ allow })` **显式开启**（P01：未登记即拒）。只声明能力不会顺手打开白名单 —— 否则「只想限定日期格式化用 dayjs」会等价于「其余运行时依赖全部未批准」。两者不冲突：只要 `allow` 非空，能力首选必须同时登记在 `allow` 里，不一致由 `policyConflicts` 直接拦下（见 ADR-0005）。

### 12.2 两档证据 —— 零误报的关键

| 档                    | 判据                                                                                                                         | 处置                 |
| --------------------- | ---------------------------------------------------------------------------------------------------------------------------- | -------------------- |
| **强指纹**            | 形态无歧义：`JSON.parse(JSON.stringify(x))`、`Math.random().toString(36)`、手搓 `process.argv.indexOf`、`toLocaleDateString` | 单证据即 **error**   |
| **弱指纹 + 命名指纹** | 自研模块同时命中弱形态（`setTimeout` + `clearTimeout`）与 ≥2 个库 API 名（`debounce` / `throttle`）                          | **warn**             |
| **允许自研的能力**    | `allowOwn: true`（例如 query string 小工具）                                                                                 | 只提示               |
| **项目覆盖**          | `deps({ fingerprints })` 对某个能力加 / 删指纹、改 API 名、放宽 `allowOwn`（企业规范收紧、自研库其实成熟）                   | 判定跟着**生效表**走 |

指纹表（能力 / 首选方案 / 语法指纹 / API 名 / 推荐写法）在 `data/wheel-fingerprints.ts`，**纯数据，引擎零库名**。

**检测过程**（P06）：

1. 只对**项目声明过的能力**生效（`deps({ capabilities })`）——项目不需要日期处理，就不会被要求用 dayjs。
2. 逐文件逐行扫该能力的**强指纹**正则，命中记 `{文件, 行}`（每文件只记首个）。
3. 判断"是否已经在用登记方案"：平台内置（`structuredClone` / `Intl` / `URLSearchParams` / `crypto.randomUUID`）视为满足；否则看**这个文件自己**有没有 import 首选库。
   **为什么按文件判**：按全项目判会放过「部分迁移」—— A 文件已经改用 dayjs、B 文件还在手搓，全项目判定看到 dayjs 就用过就全放过了；而按文件判，B 文件照样被点名（封装在 A 文件里的合法写法不会命中指纹，因此不会误报）。
4. 未满足 → 每个命中文件一条 error，附推荐写法；`allowOwn: true` 的能力降级为 warn。
5. **注释先遮罩**再匹配（否则一句"别用 `JSON.parse(JSON.stringify(x))`"的注释就会触发），无 `package.json` 的项目整体跳过依赖类规则。

配套的反向检查：**能力层**的 P08（登记了首选方案却全项目零引用）**已委派** knip / depcheck；
**适配表层**由 **P11** 实现（适配表声明的库既没 import、也没有 vendor 选择器/变量 → warn）。

### 12.3 规则

| ID  | 红线                                                                                                           | 等级 | 级别  |
| --- | -------------------------------------------------------------------------------------------------------------- | ---- | ----- |
| P06 | **能力必须用登记方案**：命中强指纹却未使用该能力的首选库（`platform: true` 的能力没有 import 可查 → 命中即报） | L2   | error |
| P07 | **疑似自造轮子**：弱指纹 + 自研模块导出名与库 API 名**精确同名**（一个即可）                                   | L2   | warn  |
| P11 | **适配表声明的库必须真的被用**：既没 import、也没有 vendor 选择器/变量（本体实现的反向检查）                   | L2   | warn  |

> **已并入 / 已委派**（不再单列）：**P09**「平台内置优先」并入 **P06**（就是 `platform: true` 的条目）；
> **P10** = **P01** 的 fail-closed 表述；**P03**（幽灵依赖）与 **P08**（能力库死依赖）委派 knip / depcheck，本体不实现。

**实现状态**（以 `--stats` 与 `deps()` 预设的 `enable` 为准）：

| 状态               | 规则                                                                                                   |
| ------------------ | ------------------------------------------------------------------------------------------------------ |
| 已落地（本体实现） | **P01 · P02 · P04 · P05 · P06 · P07 · P11**（`packs/core/rules/deps.ts` · `deps-adapters.ts`）         |
| 已委派（不在本体） | **P03** 幽灵依赖、**P08** 死依赖 → knip · depcheck（§4.9）；P10 是 P01 的 fail-closed 表述，不另立规则 |
| 已并入             | 原设计的 **P09**（平台内置优先）并入 P06 —— 平台类能力命中即报，不需要"import 过"                      |

`--verify-deps` 做的是**本地对账**（适配表声明的包 vs `package.json`）。

### 12.4 边界：不许变成「禁止写工具函数」

- 只对**已知能力**生效 —— 项目特有逻辑（业务 ID 格式化、领域计算、专用解析）永远不受限。
- 自研的**位置**仍受目录契约约束（`shared/lib` 或域内 `lib/`），但**内容不被禁止**。
- 需要零运行时依赖的项目（浏览器插件、SDK）用**规则级例外**逐条声明并写明理由：例外可见（每次运行都点名），不是悄悄绕过。
